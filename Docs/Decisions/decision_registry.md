# Decision Registry — Disclosure Drift

**Purpose:** a single index of every record in `Docs/Decisions/`, its current status, and any
supersession relationship between records. This registry does not itself change any frozen
definition. It reports what the existing decision records already say.

**Created:** 2026-07-27, during the M2.3 governance-repair exception (Decision 010 audit finding
C10: "No decision registry exists"; corrected-plan §7.5). Authority for creating this file is the
scoped documentation exception described in `Milestones/milestone_2_3_pilot_selection_plan.md`
§"Documentation authorization note".

**How to use this registry:** when two records appear to address the same topic, the record with
the later **Supersedes**/**Superseded by** relationship controls for the specific point it names.
A supersession is always partial unless the superseding record says otherwise — it never implies
the earlier record is wrong or withdrawn on any point it does not name.

## Index

| ID | Title | Date | Status | Supersedes | Superseded by | Governs |
|---|---|---|---|---|---|---|
| 001 | Milestone 0 Novelty Boundary | 2026-07-25 | Accepted working decision; final literature refresh still required | — | — | Milestone 0 |
| 002 | Primary Outcome and Company Universe | 2026-07-25 (approved and clarified 2026-07-27) | Approved by project owner, with a 2026-07-27 primary-universe-boundary clarification (`primary_universe_eligible` flag, engineering-only financial pilot entities, XBRL concept-hierarchy freezing rule) | — | — | Primary outcome definition |
| 003 (v0.1, archived) | Temporal Split and Holdout Protection | 2026-07-25 | Proposed freeze pending project-owner approval (draft; superseded by v0.2) | — | 003 (v0.2, live) | — |
| 003 (v0.2, live — `decision_003_temporal_split.md`) | Temporal Split and Holdout Protection | 2026-07-25 | Approved by project owner; **cohort date-source rule superseded in part** | 003 draft v0.1 | 010 (date-source rule only) | Cohort windows, maturity gates |
| 003 (v0.2, archived duplicate — `decision_003_temporal_split_v0_2.md`) | Temporal Split and Holdout Protection | 2026-07-25 | Unmodified historical snapshot — identical to the live 003 record as it stood before the 2026-07-27 governance-repair banner was added; left untouched by that repair | — | 010 (date-source rule only, via live 003) | — |
| 004 | Evaluation, Inference, and Rewrite Protocol | 2026-07-25 | Proposed freeze pending project-owner approval | — | — | Evaluation protocol |
| 005 | 2025 and 2026 Recency Extension | 2026-07-25 | Approved by project owner | — | — | Recency-cohort evaluation timing |
| 006 | Final Contribution Boundary | 2026-07-25 | Approved for Milestone 0 completion | Provisional contribution language in earlier charter versions | — | Milestone 0 completion |
| 007 | SEC Universe and Issuer Identity | 2026-07-25 | Approved by project owner | — | — | Milestone 2 onward |
| 008 | Filing Inventory and Amendment Policy | 2026-07-25 | Approved by project owner | — | — | Milestone 2 onward |
| 009 | Raw-Data Governance and Storage Architecture | 2026-07-25 | Approved by project owner | — | — | Milestone 2 onward |
| **010** | **Temporal Availability and Cohort Assignment** | 2026-07-25 | Approved by project owner | 003 (date-source rule only) | — | Milestone 2 onward — **controlling record for cohort date-source assignment (`official_filing_temporal_cohort`)** |
| 011 | EDGAR Operating-Calendar Provenance | 2026-07-26 | Approved by project owner | — | — | Stage M2.2 onward |
| 012 | Accession Observation Resolution | 2026-07-26 | Approved | nothing (extends 008, 010, 011) | — | Milestone 2, Stage M2.2-R2.3 |
| 013 | M2.3 Pilot Selection Mechanics (as-of cutoff, candidate storage, counting units, multi-registrant accounting, selector policy, reserves/substitution, manifest hashing, approval semantics) | 2026-07-27 | Approved by project owner | — | — | Milestone 2.3 onward |
| 014 | M2.3 Pilot Evidence Levels and Classification Policy (evidence-level taxonomy, filer-size classification, industry assignment and SIC-family mapping, stable/eventful history, amendment-purpose categories, provisional cohort assignment) | 2026-07-27 | Approved by project owner. §4 (SIC-to-industry-family mapping) was approved as a draft pending owner review on 2026-07-27 and was frozen as `sic-family-mapping/0.2` on 2026-07-27 during the same-day governance-repair correction pass. | — | — | Milestone 2.3 onward |
| 015 | M2.3 Pilot-Use Prohibition | 2026-07-27 | Approved by project owner | — | — | Milestone 2.3 onward; see `Docs/leakage_register.md` L19 |
| 016 | M2.3 Schema and Artifact Architecture (Stage S3 candidate/selection/manifest table family, ID scheme, lifecycle rules, integrity constraints, reserve-package signature model, hash boundaries) | 2026-07-27 | Approved by project owner | — | — | Milestone 2.3, Stage S3 onward (design only; no implementation authorized) |
| 017 | S4 Quota Policy Version and Boundary-Control Evidence Interpretation (frozen `PILOT_QUOTA_POLICY_VERSION`, `excluded_pool_count` definition, boundary-control structural-evidence interpretation, confirms Decision 013's objective is unchanged) | 2026-07-28 | Approved by project owner | — | — | Milestone 2.3, Stage S4 onward |
| 018 | [M2.3 Stage S5 Accession Selection Policy](decision_018_m23_s5_accession_selection_policy.md) (applicability-aware evidence penalty within the unchanged Decision 013 §5 objective; accession roles, caps, and entity accession floors; canonical dashed accession identity and tie-break formula; deterministic `selected_order`; S4-draft disposition and a distinct content-derived S5 joint run; accession families and linked-amendment coverage; fiscal-year-end and name-change derivation; 2009/2010 pairing; controlled deferral of the unmeasurable difficult-or-nonstandard-package quota; node-limit and failure semantics; retry prohibition; S5.1/S5.2 methodological boundary; future `PILOT_JOINT_SELECTOR_POLICY_VERSION`, future additive migration `0011`, and five future reason codes) | 2026-07-28 | Approved by project owner | — | — | Milestone 2.3, Stage S5 onward (policy only; **authorizes no implementation**) |
| 019 | [M2.3 Stage S5 Frozen-Storage-to-Pure-Input Mapping Policy](decision_019_m23_s5_storage_to_pure_input_mapping.md) (candidate-snapshot representation and loader-conversion rules for the four S5.1 pure inputs with no stored column: amendment-linkage evidence, multi-registrant evidence, explicit pre-study support provenance, and former-name identity evidence; snapshot-freeze validation obligations; run-identity content) | 2026-07-28 | **APPROVED — OWNER APPROVED 2026-07-28** | — | — | Milestone 2.3, Stage S5.2 onward (**clarifies** Decision 018 §§5.3, 13, 15, 19, 25; binding; authorizes no implementation — a separate bounded S5.2 implementation prompt is required) |
| 020 | [M2.3 Stage S5.4 Reserve Architecture and Quota-Contribution Membership](decision_020_m23_s5_4_reserve_architecture.md) (recommended architecture for reserves; quota-contribution membership published from the sole accepted S5.1 witness derivation; the single-`running`-window write boundary migration `0009`'s triggers impose; reserve purpose, target coverage, one rank-1 package per target, eligible pool, tie-break, signature and exact-equality rule, cross-target replacement reuse; identity and hashing separation with the input schema version unchanged; fail-closed failure and reconstruction rules; one authorized reason code; the nine owner rulings recorded, plus the §8.3 owner test-scoping clarification assigning each invariant to its owning enforcement layer; **and one future additive migration `0012_m23_selection_entity_reasons.sql` authorized in principle — one new STRICT `pilot_selection_entity_reasons` table plus four triggers whose complete SQL is frozen in §8.2: fail-closed INSERT/UPDATE/DELETE lifecycle guards with immutable target identity and an OLD-and-NEW running check, and an additive feasible-transition disposition-completeness trigger — not created by the record, editing no existing migration, and the only migration authorized for the stage**) | 2026-07-29 | **APPROVED — OWNER APPROVED 2026-07-30** | — | — | Milestone 2.3, Stage S5.4 onward (**interprets and extends** Decision 018 §§19, 22, 29 and operationalizes Decision 013 §6 and Decision 016 §7; binding). **Stage S5.4 is implemented, independently accepted, and checkpointed — final independent recommendation `ACCEPT_M23_S5_4_FOR_CHECKPOINT`, owner-accepted 2026-07-30, accepted suite 1899 passed and 1 skipped, migration `0012` created and accepted, twelve implementation paths delivered, five accepted methodological limitations recorded in §19.1, tagged `m2.3-s5.4-complete` supplementing the immutable `m2.3-s5-complete`. S6 was excluded from S5.4 and is now separately accepted under Decisions 021–023; no S5 selection or reserve is a published or owner-approved input. It authorizes no further implementation — a new explicit owner authorization is required for any future S5.4 change.** |
| 021 | [M2.3 Stage S6 Manifest Construction, Terminal Result Identity, and the Publication Boundary](decision_021_m23_s6_manifest_construction.md) **(v0.5, 2026-07-30)** (the exact canonical preimage of `selection_result_sha256` and of all eight manifest component hashes plus `root_manifest_sha256`; the four terminal component boundaries, with the migration-`0012` reserve dispositions bound into `reserves_sha256` and reserve child rows hashed directly; the source-content, candidate-table, quota-definition, and **eleven-field** selector-policy allowlists, with dependency-lock, code-commit, Python-runtime, configuration, decision-authority, and source-plan identity as six required explicit arguments never inferred from Git, the environment, the interpreter, or the working tree; the `manifest_id` derivation from root/ordinal/supersedes and the **immutability after insertion of all six manifest identity fields**; eight frozen circularity exclusions plus the §10.1 commitment closure; fail-closed manifest eligibility; the proposed-only boundary and the separation of deterministic finalization from release candidacy, approval, and publication; **the complete pilot-manifest document contract — thirteen mandatory blocks operationalizing milestone plan §10, with the rule that no substantive serialized field may be unbound by the root, S6 defining and fixture-testing the schema and S9 supplying the exact real-data instance, **and the exhaustive item-by-item §10 crosswalk in §13.2.1 covering all 81 atomic §10 items with a frozen machine-checkable count — 42 direct, 30 transitive, 8 operationally excluded, 1 deferred to S9, 0 deferred to S10, and 0 unclassified**; the **five-column** structural-fingerprint partition rule — `region`, `state`, `observed_type`, `member_name`, `record_path`, partitioned by `parser_run_id`, reduced to a distinct set per run, required equal across runs and failing closed otherwise, hashed once, with `parser_run_id` excluded — making an identical reparse, a duplicate identical row, and any ordering change provable digest no-ops while every one of the five fields stays load-bearing; explicit classification of six residual schema columns; canonical JSON under `DataTree.releases / "pilot"` with a content-derived filename; the deliberate narrowing of milestone plan §16's M2.3A-6 CLI element to Stage S9; **and one future additive migration `0013_m23_manifest_lifecycle_guards.sql` authorized in principle — DDL-only, eight new triggers and no table, column, or index, whose complete SQL is frozen in §15.1 with per-block and concatenation SHA-256 digests, byte counts, line counts, and the exact concatenation rule in §15.3: a `pilot_selection_runs` INSERT guard refusing any pre-sealed row, an append-once `selection_result_sha256` UPDATE guard permitting a seal only on a feasible run and refusing every later change or clearing while leaving an identical rewrite idempotent, a fail-closed `pilot_manifest_versions` INSERT guard requiring an existing feasible run with a matching snapshot and a sealed digest, an OLD-and-NEW UPDATE guard holding all six manifest identity fields immutable with NULL-safe comparisons, a fifth BEFORE INSERT replacement guard closing all three uniqueness routes by which INSERT OR REPLACE could displace or impersonate an existing manifest — including an owner-approved one — and, added at v0.5, a sixth BEFORE INSERT replacement guard and a seventh unconditional BEFORE DELETE guard on `pilot_selection_runs` plus an eighth BEFORE UPDATE guard holding `selection_run_id`, `snapshot_id`, and `selection_input_sha256` immutable, all independent of any pragma; not created by the record, editing no existing migration, and the only migration authorized for the stage**, **together with the §15.5 append-once and identity guarantee: a run is inserted only unsealed, can never be replaced or deleted, cannot have its persisted identity changed, seals only through the guarded update on an already-feasible run, cannot have that seal changed or cleared, tolerates identical restatement, and therefore carries a `selection_result_sha256` that is append-once **and remains recomputable from its persisted preimage** across every direct SQLite write path) | 2026-07-30 | **ACCEPTED — OWNER APPROVED 2026-07-30** | — | — | Milestone 2.3, Stage S6 onward (**interprets and extends** Decision 018 §22 and operationalizes Decision 013 §§7–8, Decision 016 §§5, 8, and milestone plan §10; **binding**). Records the project owner's S6 rulings and freezes the resulting architecture. **v0.2 applied six bounded owner corrections issued after the focused independent governance review of v0.1** (identity immutability; the fourth trigger and restated SQL and digests; the full document contract; the fingerprint rule; the residual exclusions; the CLI and S7–S10 boundary); **v0.3 applied one further correction, to the fingerprint only** — the structural tuple widened from three columns to five; **v0.4 applied two further corrections issued after the focused independent governance review of v0.3, which returned `REQUIRES_OWNER_CLARIFICATION`**: (A) the exhaustive item-by-item milestone-plan §10 crosswalk in §13.2.1, replacing a summary that left nineteen §10 items and four partial items neither serialized nor classified, together with the correction of §13.2's "twelve blocks" heading over a thirteen-row table; and (B) migration `0013` grows from four triggers to five, because the review demonstrated that six-field identity immutability held on the UPDATE path only and that INSERT OR REPLACE rewrote a manifest row wholesale past every guard, including over an owner-approved manifest. and **v0.5 applies one further owner ruling issued after the focused independent governance review of v0.4, which also returned `REQUIRES_OWNER_CLARIFICATION`**: that review accepted the §10 crosswalk and the five-trigger manifest design and proved by direct probe that `pilot_selection_runs` was still open to row replacement, deletion, and identity mutation, so migration `0013` grows from five triggers to **eight**, the five v0.4 blocks are retained byte-for-byte with their per-block digests, trigger 2 is neither widened nor renamed, and §19.11 is **closed**. **v0.1, v0.3, and v0.4 were each independently reviewed and none was approved; v0.2 was never independently reviewed**; all five are the same unapproved record, so nothing downstream is invalidated. v0.5 changes no preimage, no classification, and no `manifest_id` rule; **the v0.4 five-block statement region, its 7436-byte and 129-line counts, and its concatenation digest `6bfb897c…` are withdrawn as a composition** — the individual digests of blocks 1–5 are **not** withdrawn — and replaced by an eight-block region of 10939 bytes over 186 lines. **Accepted 2026-07-30** on the focused independent governance review of v0.5, which reached its own conclusion, reproduced all nine §15.3 digests from the record bytes, applied the extracted SQL to a scratch `0001`–`0012` catalog, ran 318 adversarial assertions across all four `recursive_triggers` × `foreign_keys` combinations, and returned `ACCEPT_DECISION_021_V05_FOR_OWNER_APPROVAL` with **no governance blockers and no owner clarifications required**. It produced one editorial correction to §13.2.1's explanatory arithmetic — **74 original bullets producing 81 atomic requirements** (74 + 7 compound splits = 81) — which changes no crosswalk row, numbering, category total, digest preimage, or SQL byte. **It authorized no implementation by itself**, and all three gates have since closed: the independent review of v0.5; owner approval recorded here; and the separately issued bounded S6 implementation authorizations, which were issued and exercised. **Stage S6 is now implemented, independently accepted, and checkpointed** at `m2.3-s6-complete` — see **Decision 023**, which records that acceptance and is the controlling record for it. `Milestones/contracts/m23_s6.md` is now `ACCEPTED_AND_COMPLETE` with `IMPLEMENTATION_AUTHORIZATION: NO` and authorizes nothing further. S7–S10 (Gate F safety and allowlist, live metadata, the frozen candidate snapshot with the real-data manifest and CLI output, owner approval of the exact root hash) remain excluded, unauthorized, and not begun; the Milestone 2 / Milestone 3 boundary reorganization and the full Milestone 2 integrated review are separate later sessions, and Milestone 2 is not closed by S6 acceptance |
| 022 | [M2.3 Stage S6 Reserve-Rank Applicability (crosswalk item 46)](decision_022_m23_s6_reserve_rank_applicability.md) (the owner clarification of Decision 021 §13.2.1 item 46: reserve rank is applicable **once per persisted compatible reserve package**, and is **structurally not applicable** for a selected target that instead carries the persisted `REVIEW_PILOT_NO_COMPATIBLE_RESERVE` disposition; structural non-applicability does not make a feasible S5 run manifest-ineligible; **item 70 remains the total reserve-coverage requirement** — every selected target covered by exactly one rank-1 package or exactly one no-compatible-reserve disposition; a zero-package run is manifest-eligible only when the selected-target set is complete, the reserve-package family is present and empty, every target has exactly one disposition, every disposition is `REVIEW_PILOT_NO_COMPATIBLE_RESERVE`, and none is extra, duplicate, conflicting, or missing; mixed package/disposition runs remain valid; and **no synthetic package, `reserve_rank = 0`, `null`, `"N/A"`, placeholder, or invented rank may be created or serialized**) | 2026-07-31 | **ACCEPTED — OWNER APPROVED 2026-07-31** | — | — | Milestone 2.3, Stage S6 onward. **Clarifies Decision 021 §13.2.1 item 46 only; it supersedes and amends nothing.** Issued after a fresh independent S6 implementation audit found that a lawful, accepted, feasible, sealed S5 run with zero compatible reserve packages and one `REVIEW_PILOT_NO_COMPATIBLE_RESERVE` disposition per selected target — the shape Decision 020 §7.1 rules nonblocking and migration `0012` accepts as complete — passed all seven Decision 021 §11.2 eligibility conditions but was refused at document verification because item 46's `reserves.packages[].reserve_rank` leaf does not exist with zero packages. That audit correctly stopped under Decision 021 §§21 and 13.3 and returned `REQUIRES_OWNER_CLARIFICATION` rather than choosing a resolution. The owner adopted the applicability rule and **rejected** both making zero-package runs ineligible (it reverses Decision 020 §7.1 and Decision 021 §11.2, and would reward the discretionary substitution Decision 013 §6 forbids) and inventing a placeholder rank (it would serialize a value no persisted row contains and no digest legitimately commits, violating Decision 021 §13.3). **Item 46 stays numbered 46, classified `D`, in block 12, bound to `reserves_sha256`;** the 81-item crosswalk and its counts (D 42 / T 30 / X 8 / S9 1 / S10 0 / unclassified 0), every digest preimage, `manifest_id`, canonicalization, migration `0013`'s bytes, its nine digests, and its eight triggers are all **unchanged**, as are accepted S4 and S5 behaviour. Authorizes one bounded correction in `release/pilot_manifest.py`, `sec/pilot_manifest_store.py`, `tests/unit/test_m23_pilot_manifest.py`, and `tests/unit/test_m23_pilot_manifest_store.py`, and introduces **no Stage-S7 authority**. **Not accepted by its own completion**: a fresh independent S6 rereview and a separate final S6 acceptance review must both pass first |
| 023 | [M2.3 S6 Acceptance, Forced-Consequence Path Ratification, and Residual Limitations](decision_023_m23_s6_acceptance_and_path_ratification.md) (formal owner acceptance of Milestone 2.3 Stage S6 on the final independent recommendation `ACCEPT_M23_S6_FOR_OWNER_ACCEPTANCE_RECORDING`, formal outcome **`M23_STAGE_S6_ACCEPTED_AND_COMPLETE`**; the eighteen accepted S6 capabilities from deterministic manifest construction through migration-`0013` lifecycle enforcement, S4 isolation, S5 preservation, and Stage-S7 exclusion; **retroactive ratification of three forced-consequence test paths** — `tests/unit/test_storage_catalog.py`, `tests/unit/test_m23_entity_selection_store.py`, and `tests/unit/test_m23_accession_selection_store.py` — each an unavoidable consequence of authorized migration `0013`, verified to change no production path, no S4 or S5 methodology, and no assertion's strength, with the rewritten corruption fixtures **narrower and more fail-closed** than the code they replaced; **four accepted nonblocking limitations O1–O4** — an empty sole-carrier crosswalk family fails closed and is a future owner ruling, the release root is assumed owner-controlled, atomicity governs newly created artifacts only, and item-46 enforcement is consistent defence in depth; and authorization of exactly one commit, one push, the annotated tag `m2.3-s6-complete`, and one tag push) | 2026-07-31 | **ACCEPTED — OWNER APPROVED 2026-07-31** | — | — | Milestone 2.3, Stage S6 acceptance and the S6 checkpoint. **Supersedes and amends nothing.** Decisions 021 and 022 both remain `ACCEPTED`, unchanged, and controlling — 021 for the S6 architecture, 022 for crosswalk item-46 applicability. **This record adds acceptance, ratification, limitations, and checkpoint authorization; it adds no architecture and reopens no ruling.** Independently confirmed unchanged at acceptance: all **81** crosswalk rows and their numbering; the classification totals **D 42 / T 30 / X 8 / S9 1 / S10 0 / unclassified 0**; every accepted hash preimage; all **nine** migration-`0013` digests over a **10939**-byte, **186**-line statement region byte-identical to Decision 021 §15.1; all **eight** triggers; migrations `0001`–`0012`; and accepted S4 and S5 behaviour. **Grants no Stage-S7, S8, S9, S10, or Milestone 3 authority.** The Milestone 2 / Milestone 3 boundary reorganization is deferred to a separate governance-only session, the final integrated Milestone 2 audit to after that session, and **Milestone 2 is not closed by this record** |
| 024 | [Milestone 2 Completion Boundary and Milestone 3 Obligation Transfer](decision_024_m2_m3_boundary_governance.md) (accepted M2.3 Stage S6 — commit `5c53412d…`, annotated tag `m2.3-s6-complete`, outcome `M23_STAGE_S6_ACCEPTED_AND_COMPLETE` — is the **final implementation stage of Milestone 2**; the final Milestone 2 scope is M2.1 offline SEC policy/storage/provenance/governance, M2.2 controlled live-metadata readiness, and M2.3 through accepted S6; **Milestone 2 is implementation-complete but NOT formally closed** and remains open only for the final independent integrated Milestones 1–2 audit, bounded correction, fresh rereview where required, and formal closeout; **the obligations previously described as M2.3 S7–S10 move into Milestone 3 intact** as **M3.1** controlled live-operation readiness with Gate F, **M3.2** controlled metadata-only SEC acquisition with Gate H, **M3.3** frozen real pilot snapshot and deterministic execution carrying the CLI output deferred from S6, and **M3.4** exact root-hash owner approval, with new **M3.5** integrated real-pilot acceptance and Milestone 3 checkpoint; a seven-column traceability table records every phase's inherited gates, inherited prohibitions, required owner decision, required validation, and implementation-authorization status; **no gate, prohibition, owner ruling, validation requirement, identity, methodology, or accepted limitation is removed, weakened, renumbered, or silently rewritten by the move**) | 2026-07-31 | **ACCEPTED — OWNER APPROVED 2026-07-31** | — | — | The Milestone 2 / Milestone 3 boundary, the obligation transfer, the Milestone 3 phase map, and the integrated-audit handoff. **Supersedes and amends nothing**; Decisions 021, 022, and 023 all remain `ACCEPTED`, unchanged, and controlling for what they govern. **Formal outcome `M2_M3_BOUNDARY_GOVERNANCE_ACCEPTED`.** **Grants no implementation authority** — assigning an obligation to Milestone 3 is not authorization to begin Milestone 3, implementation authorization is `NO` for every phase, and no phase may begin without a separate accepted governance record where required, a bounded implementation contract, explicit owner authorization, exact path authorization, and satisfaction of its inherited prerequisite gates. Milestone 3 implementation additionally may not begin before Milestone 1 and Milestone 2 closeout is complete. Milestone 3 inherits every applicable accepted control unchanged — frozen cohort windows and cutoffs, seed `20260725`, official filing-date authority, acceptance-date audit-only treatment, plain/dashed accession rules, SEC user-agent, rate limiting, response policy, raw-store and provenance rules, schema-drift rules, CompanyFacts-disabled, Frames API prohibited, external corpora validation-only, the leakage register including L01/L04/L10/L18, accepted S4 isolation, accepted S5 and S6 behaviour, Decisions 013–024, and all accepted nonblocking limitations. It changes no cohort boundary, cutoff, seed, SEC policy, identifier, temporal policy, leakage control, selection methodology, reserve, disposition, hash preimage, manifest identity, migration byte, or S4/S5/S6 behaviour. **Authorizes one governance-only commit and one push; no tag** — the closeout session controls closeout tags. **Next authorized action: `FINAL_INDEPENDENT_INTEGRATED_MILESTONES_1_AND_2_AUDIT`** |
| 025 | [Integrated Audit Documentation Corrections and Independent Verification Handoff](decision_025_integrated_audit_documentation_corrections.md) (records the final independent integrated Milestones 1–2 audit result **`REQUIRES_BOUNDED_INTEGRATED_FIXES`**, with **nine categories confirmed `INTEGRATED_ACCEPTANCE_CONFIRMED`** — Milestone 1, M2.1, M2.2, M2.3, Milestone 2 integrated, governance, reproducibility, security and leakage, and test adequacy — the Milestone 3 boundary `GOVERNANCE_READY_IMPLEMENTATION_NOT_AUTHORIZED`, and the single `PROJECT_DOCUMENTATION_CLASSIFICATION: REQUIRES_BOUNDED_FIX`; the exact documentation defect — `Docs/sec_data_dictionary.md` declared a scope covering the operational SQLite catalog while documenting only migrations `0001`–`0008`, omitting the **21 `pilot_*` tables of migration `0009`**, the `0010`/`0011` policy rows, the **one further table and four triggers of `0012`** bringing the catalog to **22 `pilot_*` tables**, and the **eight triggers of `0013`**; the navigation defect that `Docs/preregistration.md` §25 was not clearly reachable as the deviation register; the authorized corrections; and the **independence disclosure** that the same conversation authored Decisions 023 and 024) | 2026-07-31 | **ACCEPTED — OWNER APPROVED 2026-07-31** | — | — | The bounded documentation corrections and the verification handoff. **Supersedes and amends nothing**; Decisions 021, 022, 023, and 024 all remain `ACCEPTED`, unchanged, and controlling. **Formal outcome `INTEGRATED_AUDIT_DOCUMENTATION_CORRECTIONS_AUTHORIZED`.** **Documentation and governance recording only**: it changes no schema, migration, database behaviour, production code, test, configuration, CI, methodology, selection rule, reserve rule, manifest rule, hash preimage, accepted decision outcome, or S4/S5/S6 behaviour, does not edit `Docs/preregistration.md`, and **grants no implementation authority**. The audit found **no** implementation, methodology, migration, hashing, selection, manifest, leakage, security, or test defect. The independence disclosure establishes no technical defect but prevents closeout from resting solely on that session's governance assessment, so a **fresh session that authored none of Decisions 023, 024, 025 or these corrections must independently verify them**. **Milestones 1 and 2 remain open** until that verification passes and a separate closeout session completes. Authorizes one documentation/governance commit and one push; **no tag**. **Next authorized action: `FRESH_INDEPENDENT_INTEGRATED_CORRECTION_AND_GOVERNANCE_VERIFICATION`** |
| 026 | [Final Integrated Closeout of Milestones 0, 1, and 2](decision_026_milestones_0_1_2_final_closeout.md) (formal closeout of **Milestone 0** — research question and framing, novelty review, preregistration, frozen cohorts, frozen outcome cutoffs, bootstrap seed `20260725`, the leakage register, the deviation register and D001, and the accepted governance foundation; **Milestone 1** — repository and packaging foundation, configuration, cohort mirror enforcement, CLI and exit-code behaviour, offline safety, and secret and hygiene controls; **Milestone 2.1** — offline SEC policy, identifier and temporal policy, response and rate-limit policy, storage/provenance/schema-drift/release/forecast boundaries, and the CompanyFacts-disabled and Frames-prohibited policy; **Milestone 2.2** — controlled live-metadata readiness, SEC identity requirements, transport isolation, deterministic request governance, raw-store provenance, and offline test and CI boundaries; and **Milestone 2.3 through accepted Stage S6** — deterministic candidate and snapshot identity, entity and accession selection, reserves and dispositions, persistence, reconstruction and replay, selection-result sealing, manifest construction, canonical serialization, lifecycle enforcement, verification and atomicity, and the accepted limitations; the closeout baseline `65a57f40…`; the eleven-step review chain from the stage-level implementation reviews through the explicit Milestone 0 standalone audit; the final rereview outcome `ACCEPT_BOUNDED_FIXES_AND_AUTHORIZE_MILESTONES_0_1_AND_2_FORMAL_CLOSEOUT`; all sixteen final classifications; the completion confirmations including the migration chain ending at `0013` and the accepted suite of **2324 passed, 2 skipped**; the **inherited limitations register left active**; the nonblocking `pilot_reserves` PK-superset UNIQUE presentation observation; **authorization of the three annotated completion tags** `m0-complete`, `m1-complete`, and `m2-complete`; and the **Milestone 3 master-planning handoff**) | 2026-07-31 | **ACCEPTED — OWNER APPROVED 2026-07-31** | — | — | The formal closeout of Milestones 0, 1, and 2, the completion-tag authorization, and the Milestone 3 master-planning handoff. **Supersedes and amends nothing** — Decisions 001–025 all retain the authority they already hold, and **Decision 026 supersedes no methodology record**: Decision 021 remains controlling for the S6 architecture, Decision 022 for crosswalk item-46 applicability, Decision 023 for S6 acceptance and limitations O1–O4, **Decision 024 for the Milestone 2 → Milestone 3 obligation transfer**, and **Decision 025 for the integrated-audit documentation corrections**. **Formal outcome `MILESTONES_0_1_2_FORMALLY_ACCEPTED_AND_CLOSED`.** **Governance only**: it changes no production code, test, migration, configuration, CI workflow, `Docs/preregistration.md`, `Docs/sec_data_dictionary.md`, completed contract, hypothesis, cohort window, maturity gate, outcome definition, threshold, seed, SEC policy, identifier, temporal policy, leakage control, selection methodology, reserve, disposition, hash preimage, manifest identity, digest, crosswalk row, or S4/S5/S6 behaviour. **No accepted limitation is closed or erased by closure** — Decision 020 §19.1, Decision 021 §19, Decision 022's applicability boundary, and Decision 023 §7 O1–O4 all remain active and are inherited by Milestone 3, with O1 still a future owner-ruling condition. **Grants NO Milestone 3 implementation authority**: closure satisfies only the precondition Decision 024 §8 imposed, all five of its entry conditions still apply in full, implementation authorization remains `NO` for every phase, and no Milestone 3 contract exists or is created. Existing implementation-stage tags remain immutable and the three completion tags supplement them; `m2.3-s6-complete` remains at `5c53412d820fe20a7bd727eac333ae2fb8724cd6`. Authorizes one governance-only closeout commit, one push, three annotated completion tags, and one tag push. **Next authorized action: `MILESTONE_3_MASTER_PLANNING`** |
| 027 | [Milestone 3 Master Plan and Operational Readiness Design](decision_027_m3_master_plan_and_operational_readiness.md) **(v0.2, 2026-07-31)** (the complete Milestone 3 master plan and operational-readiness design: the exact Milestones 0–2 closeout baseline verified live — closeout commit `034bbc1f…` with the three annotated completion tags `m0-complete`, `m1-complete`, and `m2-complete` all dereferencing to it, parent `65a57f40…`, `HEAD == origin/main`, clean tree, migration chain contiguous through `0013` with the normative region at 10939 bytes over 186 lines and digest `7f473802…`; the confirmation that **Decision 024 remains controlling** for the M2 → M3 obligation transfer and **Decision 026 remains controlling** for the Milestones 0–2 closeout; the exact **M3.1–M3.5 phase map** with each phase's network permission and completion token; the **M3.1A / M3.1B planning subdivision**, which creates no new milestone and no new phase — M3.1A the offline operator-workflow rehearsal at network permission `NONE`, M3.1B Gate F and zero-request readiness at `ZERO LIVE REQUESTS`, and no tag for M3.1A; the **operator-runbook requirement** with every command labelled `AVAILABLE NOW` or `PLANNED — NOT YET IMPLEMENTED`; the **mandatory complete offline rehearsal before the first SEC request**; the **execution-receipt requirement for every later live command**; the **frozen eight-template operational set**; the **Milestone 3 limitations-register requirement**, seeded with every inherited limitation and closing none; the **sequential model and validation policy** — Opus Max for architecture, contracts, owner decisions, consequential methodology, focused independent reviews, exact-root approval preparation, and final integrated acceptance, Sonnet High/Max for bounded implementation, and Haiku nowhere on the critical path, with targeted checks during implementation, one full suite and every repository gate at each phase end, independent Opus review only at consequential boundaries, and the final integrated acceptance review at M3.5; the **one-implementation-commit-per-phase default**, with an intermediate checkpoint only where a phase plan explicitly justifies it and the owner separately authorizes it; the **focused independent-review policy** with no repeated broad Milestones 0–2 audits and no reviewer reviewing work it wrote; the confirmation that **request-volume values may not be invented** — where derivable they are computed from accepted offline inputs and reproduced, and where a real candidate set or current SEC state is required they are written `EXACT_COUNT_RESOLVED_BY_GATE_F_ZERO_REQUEST_PLAN` with the exact formula, every count dependency, the future zero-request planning command, a hard request ceiling, and mandatory explicit owner approval before network enablement; the confirmation that **operational receipts are outside the accepted S5 and S6 substantive identity graphs**; the prohibition that **no execution receipt, receipt digest, timestamp, request count, response total, path, SEC identity, or operational state may contaminate candidate identity, selection identity, `selection_result_sha256`, any component digest, `root_manifest_sha256`, or `manifest_id`**; the prohibition that **no full SEC identity, secret, personal path, raw response body, filing text, outcome value, or restricted substantive payload may appear in an execution receipt**; and the confirmation that **no Milestone 3 implementation authority is granted**) | 2026-07-31 | **ACCEPTED — OWNER APPROVED 2026-07-31** | — | — | The Milestone 3 master plan, the operational-readiness design, the operator-runbook and offline-rehearsal requirements, the execution-receipt design, the frozen operational-template set, the Milestone 3 limitations register, and the Milestone 3 sequencing, validation, model, commit, tag, and review policies. **Supersedes and amends nothing** — Decisions 001–026 all retain the authority they already hold; **Decision 024 remains controlling for the M2 → M3 obligation transfer** and **Decision 026 for the Milestones 0–2 closeout**. **Formal outcome `M3_MASTER_PLAN_AND_OPERATIONAL_READINESS_DESIGN_ACCEPTED`.** **Governance and documentation only**: it changes no production code, test, migration, configuration, CI workflow, `Docs/preregistration.md`, `Docs/sec_data_dictionary.md`, completed contract, hypothesis, cohort window, maturity gate, outcome definition, threshold, seed, SEC policy, identifier, temporal policy, leakage control, selection methodology, reserve, disposition, hash preimage, manifest identity, digest, crosswalk row, or S4/S5/S6 behaviour, and creates **no runtime code, no CLI surface, no database table, and no implementation contract**. **GRANTS NO MILESTONE 3 IMPLEMENTATION AUTHORITY** — planning a phase is not authorization to begin it, all five Decision 024 §8 entry conditions still apply in full to every phase, and implementation authorization remains `NO`. Negative confirmations verified live: no SEC network access occurred, no live metadata was acquired, no real candidate snapshot or real pilot-selection run exists, no real manifest exists, no root was approved, nothing was published, no offline rehearsal has been run, no Gate F or Gate H has passed, and no migration beyond `0013` exists. Authorizes **one planning/governance commit and one push; no tag**. **v0.2 applies eleven bounded owner corrections issued after the required independent review of v0.1**, recorded in its §0 revision history and superseding only the affected v0.1 operational-planning language: (1) M3.1 rehearses **acquisition only** — the snapshot, selection, reserve, sealing, manifest, and root scenarios move to **M3.3A**, under the frozen rule that **no scenario may be placed in a phase that lacks the production path it exercises**; (2) M3.2 becomes **two sequential windows** — M3.2A bootstrap, then transport disabled, objects frozen, dependent references **derived** from them, a second zero-request plan, and a **second exact owner approval**, then M3.2B — with **the 10% contingency withdrawn**; (3) **M3.3A** implements the candidate-snapshot builder and rehearses execution offline before **M3.3B** freezes anything real; (4) **M3.4 always requires a bounded contract and is never documentary** — M3.4A validates a minimal approval-recording entry point against synthetic catalogs, M3.4B invokes it once after explicit approval, and **manual SQL against the real catalog is prohibited**; (5) the v0.1 derived counts, subtotal, plan hash, and maximum-attempt total are **withdrawn** — they were faithful to the accepted planner but **not** to Decision 013 §1, and the resulting **`CURRENT_PLANNER_DISCREPANCY` is recorded, unresolved, and blocks Gate F**, with Decision 013 unchanged; (6) `A_max = 12` and `planned × 12` are **withdrawn** — maximum reachable physical attempts is **derived per route from the implemented response-policy state machine and independently tested**; (7) one receipt integrity identity only, `receipt_id = SHA256(canonical bytes with receipt_id omitted)`, with `receipt_content_sha256` **removed**; (8) every receipt field classified **required / conditionally required / prohibited by invocation mode**; (9) `rehearsal` and `dry_run` receipts report **zero actual network counts**, with simulated totals in the rehearsal evidence report; (10) a **two-layer evidence model** — the public repository tracks blank templates, planning records, the limitations register, and a new **evidence index** carrying artifact type, phase, status, SHA-256, and a non-sensitive reference identifier, while **completed operational evidence lives in an owner-controlled private evidence root outside the repository**; and (11) the claim that **any regeneration necessarily creates a new root is withdrawn as false** — unchanged governed state plus byte-identical canonical serialization produces the **same** root, an independently re-derived identical root **remains the same approved value**, and only a differing root, changed governed state, or a superseding manifest requires a new packet. **v0.2 changes no formal outcome, no acceptance, no phase in the Decision 024 §5.1 map, and no prohibition**, and creates no second numbered decision. **Next authorized action: `INDEPENDENT_M3_MASTER_PLAN_REREVIEW`** — and only after that fresh focused review passes may the owner authorize a separate session to draft the bounded M3.1 implementation contract |
| 028 | [Milestone 3.1 Readiness Corrections and Owner Rulings](decision_028_m3_1_readiness_corrections.md) (the bounded correction required after the independent Decision 027 v0.2 rereview returned `NEEDS_CORRECTION`: preserves Decision 013 §1 and classifies M3-L12 as an inherited exact-quarter-end planner defect; requires `quarterly-index-instances/2.0`; fixes A1–A12, adds the future reason codes `SEC_REQUEST_CEILING_EXHAUSTED` and `SEC_ACQUISITION_INTERRUPTED`, freezes ceiling equality, assigns read-only recovery inspection to M3.1 and repair/resume to M3.2, corrects receipts to `m3-execution-receipt/2.0`, repairs request-budget arithmetic, and records the three-layer M3-L11 protection) | 2026-08-01 | **ACCEPTED — OWNER APPROVED 2026-08-01** | Narrowly supersedes only affected Decision 027 operational-planning language | **029 — two clauses only:** §5's A6 scenario language (rehearsal-only manifest-resolution fixture) and §6's word "exactly" together with §12's closed-delta wording (one new reason code). Every other clause is unchanged and controlling | Binding for M3.1 readiness. Fresh independent rereview returned `INDEPENDENT_M3_MASTER_PLAN_REREVIEW: PASS`; formal outcome `M3_1_READINESS_CORRECTIONS_ACCEPTED`. **Governance and documentation only**: it grants no implementation or network authority. Decision 013 and Decision 024 remain unchanged and controlling. Historical next action, now discharged: draft and independently review `Milestones/contracts/m3_1.md` — that contract exists and is `ACCEPTED_READY_FOR_IMPLEMENTATION`; see Decision 029 for the current next action |
| 029 | [Milestone 3.1 Rehearsal Completeness and Reason Semantics](decision_029_m3_1_rehearsal_completeness_and_reason_semantics.md) (the bounded M3.1 remediation record: pins the `458a741c…` baseline and confirms the live remote agrees, so the cached-ref `[ahead 17]` reading is withdrawn as false; records that **no durable §17 review artifact exists anywhere** and that no review covers the current tree, so the required review is the *first* durable one rather than "the fourth", and that a fix commit never converts a prior FAIL into a PASS; **F5** — narrowly supersedes Decision 028 §5's A6 language to permit an in-memory, rehearsal-only manifest-resolution fixture for `sec_edgar_calendar_announcement` that drives the real `SecClient.fetch()` policy loop over the existing scripted transport seam, never enters `CALENDAR_EVIDENCE_MANIFEST`, never touches the operator manifest, never persists, restores the production resolver on exceptional exit, and grants **no** retrieval authority, implemented as a context-managed substitution of `require_evidence` and never as a production resolver parameter — and rules that the fixture **survives unchanged in both the empty- and non-empty-manifest branches**, because **a zero `U(route)` never waives the independent `A_reachable` witness**; **N5** — narrowly supersedes Decision 028 §6's word "exactly" and §12's closed-delta wording to register exactly one code, `OFFLINE_REHEARSAL_SCENARIO_MISMATCH`, category `integrity`, `blocks_release=true`, **`requires_manual_review=false` as an explicit owner ruling of this record**, receipt schema `m3-execution-receipt/2.0` and every field, digest preimage, and canonicalization rule unchanged and no migration created; **R3** — records as contract-conformance code work needing no supersession that the M3.1A phase token requires all four of `passed`, `complete`, `a_reachable_agrees`, **and `a_reachable_fully_tested`**, that the stored-report path must recompute rather than trust, and that `_bounds_agree()` must not accept a subset; replaces the disconnected additive retry+cooldown+redirect arithmetic with **one realizable black-box `SecClient.fetch()` witness per route** at 6/7/11 attempts with active zero-redirect probes on the four exact singleton routes; restates the ceiling as the per-route sum `Σ U(route) × A_reachable(route)` = `31 + 6m + 11q` with **no single asserted multiplier**, and corrects *q* to the count **after** catalog-satisfied exclusion; refuses in part the over-broad Decision 011 consequence — lower-precedence fallback still applies and rollover fails closed with `OPERATING_CALENDAR_UNAVAILABLE`, so **no announcement or rollover evidence is silently lost and there are no affected dates to name**; and freezes the seventeen-step end-state sequence, in which **`m3.1-complete` before Gate F is prohibited**) | 2026-08-02 | **ACCEPTED — OWNER APPROVED 2026-08-02** | Narrowly supersedes two Decision 028 clauses only — §5's A6 scenario language, and §6's "exactly" with §12's closed-delta wording | — | Binding for M3.1 rehearsal completeness, the M3.1A phase-token predicate, the one new reason code, and the first durable §17 review artifact. Formal outcome `M3_1_REHEARSAL_COMPLETENESS_AND_REASON_SEMANTICS_ACCEPTED`. **Grants no network authority, authorizes no live SEC request, creates no migration, changes no receipt schema field or digest preimage, closes no limitations-register entry, and creates no tag.** It does **not** accept M3.1 and does not authorize `m3.1-complete`. Decisions 011, 013, 024, 026, 027 remain unchanged and controlling. Next action: the §11 pre-code governance amendment, then the bounded code remediation, then the **first durable §17 review** by a non-author session |
| 030 | [Gate F Step-12 Owner Rulings and Hygiene Remediation](decision_030_gate_f_step_12_owner_rulings_and_hygiene_remediation.md) (the bounded governance-remediation and owner-interpretation record for Decision 029 §12 step 12: authorizes exactly one provably non-substantive redaction of the machine-local absolute path material in the first durable §17 review artifact's clone-provenance sentence — pre-redaction identity `73cb1eacf0fb5e29a8a1c2ea871692068caf3ebdc48cae161d6aef677ba8f3a3` remains the historical artifact identity, sanitized identity `9c40a82934ec52227202f0160d49fc5acd0e53f61af86d6f53b6e0b26e041fe3` is the current tracked identity, the verdict `M3_1_SECTION_17_REVIEW: PASS` is unchanged and occurs exactly once, Git history is not rewritten, the hygiene scanner is not weakened and no allowlist is created; rules the three request-budget response-outcome markers `EXACT_COUNT_RESOLVED_BY_GATE_F_ZERO_REQUEST_PLAN` **permitted and nonblocking**, with every budget §3 route count resolved, the expectations intentionally resolved during controlled acquisition, and no integer guessed; records **`M3-L12 GATE-F-FACING REQUIREMENT: SATISFIED`** — Gate-F-facing requirement satisfied; administrative closure deferred to the later M3.1 acceptance and checkpoint sequence — which does not block checklist preparation, the owner step-12 signature, the step-13 readiness token, or beginning Gate F after valid step-13 authorization; and records **`D023-O1: LATENT FAIL-CLOSED REFERRAL CONDITION — NONBLOCKING UNLESS TRIGGERED`**, stop-and-refer if a lawful real run ever reaches it) | 2026-08-03 | **ACCEPTED — OWNER APPROVED 2026-08-03** | — | — | Binding for the step-12 hygiene remediation, the two review-artifact identities, and the Gate F interpretation of the unresolved budget markers, M3-L12, and D023-O1. Formal outcome `GATE_F_STEP_12_OWNER_RULINGS_AND_HYGIENE_REMEDIATION_ACCEPTED`. **Governance and documentation only**: no implementation, test, script, migration, or configuration byte changed; it grants no network authority, signs no checklist, emits no readiness token, and authorizes no Gate F or M3.2 work. Next action: the owner provisions and validates the SEC contact identity, decides branch synchronization or live remote verification, and performs the final step-12 signing preflight |
| 031 | [Milestone 3.1 Acceptance](decision_031_m3_1_acceptance.md) (the master-plan §33 governance-only acceptance record, created at Decision 029 §12 step 15 under the owner's explicit step-15 authorization: records the owner's verbatim 2026-08-03 M3.1 acceptance instrument — `OWNER_M3_1_ACCEPTANCE_DECISION: APPROVED` — bound to the independent step-14 acceptance review (artifact `Docs/m3/reviews/m3_1_independent_acceptance_review_04ce708fd46dbcf1c2fc355f16325ecea9e1f47a.md`, SHA-256 `caf9f26e6a2690a05a9d6a238d5572533b858789638b35a24da06c64a4c5ae4e`, commit `24fba32413bb6c5dade60a64182e42510afe6f88`, verdict `M3_1_INDEPENDENT_ACCEPTANCE_REVIEW: PASS`); accepts the frozen implementation `970e050deb06910adcde8588101564beb7d19c74` (tree `d0c3c94cbf9128eaf0fdb1ef58179d9977d718d3`), the owner-signed Gate F checklist `34fc0567…`, the readiness-token record `b06ae373…` (readiness only), the byte-identical request plans `19be7bdc…`, the owner-approved request budget `2d453e0b…`, and the exact hard request ceiling **801**; accepts the review's three MINOR findings as nonblocking; and records the pre-tag dispositions `CLOSURE-READY PENDING STEP 16` for **M3-L11** and **M3-L12** — closing neither — with **D023-O1** unchanged as latent) | 2026-08-03 | **ACCEPTED — OWNER APPROVED 2026-08-03** | — | — | Binding for the owner's final M3.1 acceptance and its evidence bindings. Formal outcome `M3_1_ACCEPTED_AND_COMPLETE`. **Governance only**: no implementation, test, script, configuration, or migration byte changed; it creates no tag, closes no limitations-register entry, and grants no network, Gate F-execution, acquisition, or M3.2 authority. The `m3.1-complete` tag (Decision 029 §12 step 16) and the bounded M3.2 contract (step 17) remain separately owner-gated. Next action: `OWNER_AUTHORIZATION_OF_DECISION_029_SECTION_12_STEP_16_M3_1_COMPLETE_TAG` |
| 032 | [M3.2 Contract Corrections and Rereview Requirement](decision_032_m3_2_contract_corrections.md) (records the owner's verbatim 2026-08-04 correction instrument — `OWNER_M3_2_CONTRACT_CORRECTION_DECISION: APPROVED` — bound to the independent M3.2 contract review of the initial draft at `536856325f6a655416d48276c5b93848cab388e8` (artifact `Docs/m3/reviews/m3_2_contract_independent_review_536856325f6a655416d48276c5b93848cab388e8.md`, SHA-256 `fbf8c68caa8a8a102e643ad9f0ad28758b20ed368ca7928263d6f2f89d32da57`, review commit `3fbaa12d671d0000f5b608bbf6fb271f78b4673f`, verdict `M3_2_CONTRACT_INDEPENDENT_REVIEW: PASS_WITH_REQUIRED_CORRECTIONS`); adopts findings F1–F7 and directs the bounded corrections applied to the `m3_2.md` draft — **F1 (MAJOR)**: §14 separates **termination** from **successful completion**, requiring every required object (the bulk-submissions object, both ticker files, the SIC list, the calendar-year filing-calendar page, every approved announcement-manifest entry, and all 70 required quarterly-index instances) present, hash-verified, and fully provenanced, with any required-object absence enumerated in the window's receipt and expressly owner-adjudicated **before** the between-windows freeze and **before** any M3.2B budget approval, an unadjudicated-absence window being `completed_with_absences` and ineligible for the freeze or Gate H; **F2 (MAJOR)**: §16 names the single command-scoped network-enable configuration change — new key `network.m3_acquire_enabled` (default `false`) in `configs/project.yaml` mirrored by a one-field `NetworkSection` addition in `config.py`, read only by `m3 acquire --live`, with `network.enabled` remaining `false` throughout every M3.2 window and the key set `true` only in the owner-authorized window-local configuration supplied via `DISCLOSURE_DRIFT_CONFIG` — and the complete expected implementation and test surface including all six planned M3.2 commands (`m3 acquire` with `--show-scope`/`--resume-from`, `m3 derive-dependent-plan`, `m3 reconcile-requests`, `m3 show-drift`, `m3 recover`); **F3**: §12's conservative accounting rule charging a hard-interrupted in-flight request at its full per-route `A_reachable`, else `UNDETERMINED`; **F4**: §20 gates public indexing of the between-windows freeze artifacts on an authorized evidence-index vocabulary extension; **F5**: stale current-state prose corrected in `Milestones/contracts/README.md` and `Docs/decision_index.md`; **F6**: the accepted unresolved-count sentinel `EXACT_COUNT_RESOLVED_BY_GATE_F_ZERO_REQUEST_PLAN` retained unrenamed with its historical Gate-F name explained in §§5 and 15; **F7**: §18 requires a non-vacuous positive control for every critical refusal and nonchange boundary and §19 names the exact nonchange proof — and records the **procedural ruling** that the review artifact, preserved unchanged as a truthful correction review, does not satisfy the acceptance-prerequisite review because its session used two read-only fact-gathering subagents under the one-active-session restriction, so the corrected contract requires a **fresh independent rereview by one non-author session using no subagents** before owner acceptance) | 2026-08-04 | **ACCEPTED — OWNER APPROVED 2026-08-04** | — | — | Binding for the M3.2 contract corrections and the rereview requirement. Formal outcome **`M3_2_CONTRACT_CORRECTIONS_RECORDED`**. **Governance and documentation only**: it amends only the unaccepted `Milestones/contracts/m3_2.md` draft; changes no implementation, test, script, migration, template, or executable-configuration byte (the §16 naming is contract text for a future lawful T2 — `configs/project.yaml` and `config.py` remain byte-identical to the frozen accepted SHA); accepts no contract; grants no implementation, network, CompanyFacts, live-SEC, acquisition, operational-catalog, or ceiling-use authority; creates no tag; and authorizes one bounded governance commit and one normal fast-forward push. **Next action: `INDEPENDENT_M3_2_CONTRACT_REREVIEW`** — one non-author session, no subagents, then the owner's T1 acceptance decision |
| 033 | [M3.2 Correction-Pass Adjudication and Governance Cleanup](decision_033_m3_2_correction_pass_adjudication.md) (records the owner's verbatim 2026-08-04 adjudication `OWNER_DECISION_032_CORRECTION_PASS_ADJUDICATION` of the Decision 032 correction pass published at `96dea2b50b7e87243aad29032946ef8447033eb9`: the substantive corrections are accepted and findings **F1 through F7** are accepted as substantively addressed; the prior review artifact remains preserved and is expressly **not** the final independent acceptance review; a **fresh non-author session using no subagents remains mandatory**; and M3.2 implementation, network enablement, live SEC access, acquisition, operational-catalog creation, and ceiling use all remain unauthorized. It directs two bounded governance corrections — **(1)** `Docs/decision_index.md` **restored to its exact bytes at parent commit `3fbaa12d671d0000f5b608bbf6fb271f78b4673f`** because that path was outside the final authorized-path list, with the consequence recorded rather than by editing the accepted record: Decision 032 §§5.5 and 7 name the index among the F5 targets and authorized paths, but with the restoration `Milestones/contracts/README.md` is the sole navigation file carrying an F5 correction and the stale Decision-029 next-action sentence in the index **remains an open, nonblocking navigation-staleness item** requiring its own explicit path authorization; and **(2)** the exact machine-readable marker `NEXT_AUTHORIZED_ACTION: FRESH_NO_SUBAGENT_INDEPENDENT_REREVIEW_OF_CORRECTED_M3_2_CONTRACT`, with directly dependent ledger prose aligned and no claim that the rereview has begun or been authorized. It further adjudicates two procedural deviations: the published correction commit's subject `Correct M3.2 contract and record Decision 032` rather than the prescribed `Correct bounded M3.2 contract` is accepted as a **non-substantive procedural deviation**, and **no history rewrite is authorized or performed** — the published commit is not amended, rebased, reset, or force-pushed, and the correction is a forward commit per Decision 030 §10) | 2026-08-04 | **ACCEPTED — OWNER APPROVED 2026-08-04** | — | — | Binding for the adjudication of the Decision 032 correction pass and its two bounded governance corrections. Formal outcome **`M3_2_CORRECTION_PASS_ADJUDICATED_AND_CLEANED_UP`**. **Supersedes and amends nothing** — accepted **Decision 032 is not edited**, in the convention Decision 030 §10 fixes (after commit, a correction is a new dated decision record, never an in-place edit of an accepted record); Decisions 024, 027 v0.2, and 028–032 remain unchanged and controlling for what they govern. **Governance and documentation only**: it changes no implementation, test, script, migration, template, configuration, contract, review-artifact, or private-evidence byte, and `Milestones/contracts/m3_2.md` remains byte-unchanged from `96dea2b…`. It **accepts no contract**, does not authorize or begin the rereview itself, and grants no implementation, network, CompanyFacts, live-SEC, acquisition, operational-catalog, or ceiling-use authority; it authorizes one bounded governance-cleanup commit and one normal fast-forward push, and **creates no tag**. **Next action: `FRESH_NO_SUBAGENT_INDEPENDENT_REREVIEW_OF_CORRECTED_M3_2_CONTRACT`** |
| 034 | [M3.2 Contract Acceptance (T1)](decision_034_m3_2_contract_acceptance.md) (records the owner's verbatim 2026-08-04 T1 acceptance instrument — `ACCEPT_M3_2_CORRECTED_CONTRACT_AT_T1` — bound to the fresh independent no-subagent rereview of the corrected contract: artifact `Docs/m3/reviews/m3_2_corrected_contract_independent_rereview_3bf9987dd72e1531da2f678fbbef735f37aefcf4.md`, SHA-256 `91235a1a58f94692d5607908e5fa1e2e3adc11722a0a417fc6d47798f3fefacf`, rereview commit `3069b03ede9d805e9d0196a3e4c45c8cc68f42b7`, verdict `M3_2_CORRECTED_CONTRACT_INDEPENDENT_REREVIEW: PASS`, **zero BLOCKER, zero MAJOR**. Accepts `Milestones/contracts/m3_2.md` **unchanged at T1** (accepted-text SHA-256 `75e7e5a11f6e02933c878894091b4a38cef609a1568a6095b0dbb2841e23d8d3`; the Decision-034-authorized status/authority-metadata update then produced post-acceptance file SHA-256 `a5ac0e8d042d90a7cff43a476258523ab71977b4b3d50ffe6777424720ae4ab2` with no substantive provision changed); adjudicates the rereview's independence attestation, including its container-continuity disclosure, as satisfying Decision 032 §6; accepts **R1 (MINOR)** as nonblocking while making its four-part content **mandatory in the future T2 implementation-authorization packet** (item-level absent-object identity persistence; the `completed_with_absences` representation; the receipt/catalog/`m3 reconcile-requests`/Gate H linkage; frozen-receipt-schema tests); accepts **R2 (OPTIMIZATION)** as nonblocking with no contract edit; preserves every residual limitation open (the `Docs/decision_index.md` stale sentence under Decision 033 §5; the F4 evidence-vocabulary gate; all 35 open register entries; D023-O1 latent; same-device-only backups pending T4; the three unresolved response-outcome expectations; SEC-side variability); and states the authority separation — **T1 satisfied; T2, T3, T4, and each per-window T5 remain separate future owner acts, and T1 never satisfies or weakens a later gate**) | 2026-08-04 | **ACCEPTED — OWNER APPROVED 2026-08-04** | — | — | Binding for the T1 acceptance of the M3.2 contract and the R1/R2 dispositions. Formal outcome **`M3_2_CONTRACT_ACCEPTED_AT_T1`**. **Governance and documentation only**: it changes no implementation, test, script, migration, template, or executable-configuration byte; edits no accepted decision, review artifact, or `Docs/decision_index.md`; grants no T2 implementation authorization, no T3 implementation acceptance, no T4 preflight authorization, no T5 live-operation authorization, no network or CompanyFacts enablement, no live SEC access, no connectivity testing, no acquisition, no operational-catalog creation, no use of the M3.2A ceiling 801, no M3.2B execution, no Gate H execution, and no M3.3+ work; creates no tag and authorizes no push. **Next action: `PREPARE_M3_2_T2_IMPLEMENTATION_AUTHORIZATION_PACKET`** — preparation and owner review only; the packet returns to the owner for a separate explicit T2 authorization decision |

| 035 | [M3.2 T2 Staged Implementation Authorization (Stage T2.1 Only)](decision_035_m3_2_t2_staged_implementation_authorization.md) (records the owner's verbatim 2026-08-04 instrument `OWNER_M3_2_T2_IMPLEMENTATION_AUTHORIZATION: APPROVED_WITH_STAGE_LIMIT`, bound to repository baseline `8dd4a1675019a9a885b04703d18e0274173f52c3` and T2 packet revision **v2** SHA-256 `621201464ffd0e236b90aefe3cd9f587b1c4873011e32df2aef596c7ff314599`, which it **approves as the controlling implementation plan** and **preserves unchanged**; determines all five **Decision 024 §8** entry conditions satisfied for the bounded staged implementation (the required governance record exists; the contract is accepted at T1 by Decision 034; this instrument supplies the explicit owner authorization; it approves the packet §5 fifteen-path maximum envelope subject to narrower per-stage subsets; inherited prerequisite gates are satisfied); **amends the accepted contract §22** — the one-implementation-commit default is replaced by the six-stage **T2.1–T2.6** cadence: at most one commit per stage with the exact packet §6 subject, no interim commit inside a stage without a separate owner interruption ruling, each stage commit local until ChatGPT reviews and accepts it, then one normal fast-forward push, no next stage before the prior stage is reviewed/accepted/published, no combining stages, **no stage tag and no T3 tag**, and the T2.6 commit as the implementation-freeze candidate for the independent T3 review — an amendment of **staging and commit governance only** that alters no route, source, plan, ceiling, storage semantic, recovery semantic, evidence requirement, or live-operation authority; fixes the **maximum T2 envelope** as exactly packet §5 P1–P8 and T1–T7 with the declined and prohibited surfaces still prohibited and any out-of-subset need an **immediate stop for new owner adjudication**; and grants **immediate executable authority for stage T2.1 only**, bounded to exactly six paths — `configs/project.yaml`, `src/disclosure_drift/config.py`, `src/disclosure_drift/cli.py`, `src/disclosure_drift/m3/__init__.py`, `tests/integration/test_m3_cli.py`, `tests/unit/test_config.py` — permitting only the tracked-default `network.m3_acquire_enabled: false`, the one `NetworkSection` field, strict fail-closed configuration behaviour, parser and dispatch skeletons for all six M3.2 surfaces, refusal behaviour including the `m3 acquire --live` refusal skeleton, proof that **no transport can be constructed** and that M2.2 commands remain governed **only** by `network.enabled`, and the named T2.1 tests and positive controls — while **T2.1 must not** implement acquisition, storage integration, reconciliation, drift processing, recovery repair, dependent-plan derivation, receipt emission, or transport construction, and **must not invent a fake machine-readable "T3 accepted" or "T5 authorized" boolean, token, bypass, or hard-coded authorization**; records the contract's post-amendment SHA-256 `7a3fe7ff8503268c57081a45ae756989c2c2348c427842b4d2193acd04582b03`; accepts **R1** as binding where applicable and **F3** as binding for T2.4; leaves **F4** — the proposed evidence-index vocabulary additions — **expressly not accepted and open as a separate governance decision required no later than T4 and before the first affected artifact is publicly indexed**; and carries the **durability condition** that the authorization may not be acted upon until durably recorded, the contract and ledger updated, the commit pushed, and `HEAD == origin/main`, **after which a separate exact T2.1 execution packet is still required before any implementation session begins**) | 2026-08-04 | **ACCEPTED — OWNER APPROVED 2026-08-04** | — | — | Binding for the staged T2 implementation authorization, the contract §22 six-stage amendment, and the T2.1-only stage limit. Formal outcome **`M3_2_T2_STAGED_IMPLEMENTATION_AUTHORIZED`**. **Governance only**: **no executable byte changes with this record** — `src`, `tests`, `configs`, migrations, and templates remain byte-identical to the frozen accepted M3.1 SHA `970e050d…`. It **edits no accepted decision** (032, 033, 034 untouched) and preserves the T2 packet and both M3.2 review artifacts unchanged. It grants **no** T2 stage beyond T2.1, no T3/T4/T5/T6, no network or CompanyFacts enablement, no SEC connectivity testing, no HTTP request, no live SEC access, no operational-catalog creation or population, no operational use of ceiling 801, no M3.2A or M3.2B execution, no Gate H, no migration, no receipt-schema change, no new reason code, no tag, and no M3.3+ work. **Next action: `CHATGPT_ISSUANCE_OF_M3_2_T2_1_IMPLEMENTATION_PACKET`** |

| 036 | [M3.2 Stage T2.1 Acceptance and Completion](decision_036_m3_2_t2_1_stage_completion.md) (records verbatim both the owner's `OWNER_M3_2_T2_1_STAGE_ACCEPTANCE: APPROVED` instrument and the `OWNER_M3_2_T2_1_COMPLETION_RECORDING_AUTHORIZATION: APPROVED` instrument of 2026-08-04; accepts Milestone 3.2 implementation stage **T2.1 — the configuration and fail-closed command-authority layer** — at commit `7b2ffe643a2e2e600f148592fc9f8ded5695a279` (parent `9730f8b564f49b8fdba76da31cf6d2fa0b6aacc6`, tree `0ae3cb0ba8bd9484c02f8920e2ed44c30a96a87e`, subject `Implement M3.2 T2.1 authority layer`), published by one normal fast-forward push with no tag, outcome `M3_2_T2_1_IMPLEMENTATION_PUBLISHED`; records that the stage changed **exactly the six Decision 035 §8 authorized paths and no seventh**, that `tests/integration/test_no_network.py`, `tests/conftest.py`, `m3/receipt.py`, `reasons.py`, every migration, and all of `Docs/` and `Milestones/` were proven byte-identical across it, and that targeted validation was **126 passed with no skips and no xfails** alongside clean ruff, format, mypy, secrets, hygiene, and diff-check gates; confirms the delivered properties — `network.m3_acquire_enabled` added with tracked default `false`, `network.enabled` still `false` with unchanged semantics, the two switches independent in both directions with strict unknown-field rejection and no environment fallback, all six M3.2 command surfaces recognized and every one fail-closed at exit 3 without traceback, **no switch combination reaching or constructing transport** across the full six-row conjunction, **no fake T3/T4/T5/readiness/owner-authorization state**, M2.2 commands still controlled only by `network.enabled` (proven behaviourally and structurally), and no transport, catalog, receipt, evidence artifact, raw object, token, request, attempt, hostname lookup, socket operation, or SEC contact; accepts findings **M1** and **M2** as nonblocking and records optimization **O1** as corrected before acceptance; and confirms that **stages T2.2 through T2.6 remain separately owner-gated and unauthorized**, that the Decision 035 §6 fifteen-path envelope remains a ceiling with any out-of-subset need an immediate stop for owner adjudication, and that `m3/acquisition.py` does not exist. **Amends nothing** — Decision 035 is not modified in place and the accepted M3.2 contract is deliberately **preserved unchanged**, stage progress being recorded here and in the ledger rather than in the contract) | 2026-08-04 | **ACCEPTED — OWNER APPROVED 2026-08-04** | — | — | Binding for the acceptance and completion of M3.2 implementation stage T2.1. Formal outcome **`M3_2_T2_1_ACCEPTED_AND_PUBLISHED`**. **Governance only**: **no executable byte changes with this record**; it edits no accepted decision, no contract, no template, no migration, no packet, no review artifact, and no `Docs/decision_index.md`. It grants **no** T2.2-or-later stage authority, no T3/T4/T5, no network or CompanyFacts enablement, no SEC connectivity testing, no live SEC access, no acquisition, no operational catalog, no use of ceiling 801, no Gate H, no migration, no receipt-schema change, no new reason code, no tag, and no M3.3+ work. **Next action: `CHATGPT_PREPARATION_OF_M3_2_T2_2_IMPLEMENTATION_PACKET`** |

| 037 | [M3.2 Remaining Implementation-Stage Combination](decision_037_m3_2_remaining_stage_combination.md) (records verbatim the owner's 2026-08-04 `OWNER_M3_2_REMAINING_STAGE_COMBINATION_DECISION: APPROVED` instrument, issued as the "separate explicit owner decision" **Decision 035 §7 item 8** requires before any stages may be combined; consolidates the remaining T2 work into **combined T2.2–T2.3 — Catalog, Immutable Storage and Acquisition Engine** (subject `Implement M3.2 T2.2-T2.3 acquisition foundation`), **separate T2.4 — Recovery, Reconciliation and Drift Control** (subject `Implement M3.2 T2.4 recovery and reconciliation`), and **combined T2.5–T2.6 — Operator Surfaces and Integrated Implementation Candidate** (subject `Complete M3.2 T2.5-T2.6 integrated implementation`), so the cadence is four stages in total with T2.1 already complete; fixes **at most one commit per stage**, each remaining local until ChatGPT review and acceptance then one normal fast-forward push, no next stage before the prior is accepted and published, **no further combination without a new explicit owner decision**, and **no interim stage tag or T3 tag**; makes the **combined T2.5–T2.6 commit the implementation-freeze candidate** for independent T3 review, replacing the former standalone T2.6 commit; permits internal validation subphases inside a combined stage while requiring one coherent candidate, with **no owner review between the T2.2–T2.3 catalog/storage and acquisition-engine subphases unless** an authorized-path expansion, migration, new reason code, or frozen-receipt-schema insufficiency appears necessary, the accepted architecture cannot be implemented as written, or a BLOCKER or relevant MAJOR finding arises — each an immediate stop; **supersedes only the T2 packet's remaining-stage cadence and commit-boundary provisions**, leaving the packet byte-unchanged and every other packet requirement controlling; amends the accepted contract **§22** plus the consequent authority metadata, recording post-amendment contract SHA-256 `c526335b91ddb75877e66ecef3255dce6c4c27e60ae0c5a7286228935d42edb7`; and preserves every substantive boundary — the fifteen-path envelope, routes and sources, plan/budget/**ceiling 801** identities, raw-object and catalog semantics, the frozen receipt schema, recovery and completion semantics, evidence requirements, independent T3 review, T4 preflight, per-window T5, and Gate H) | 2026-08-04 | **ACCEPTED — OWNER APPROVED 2026-08-04** | Only the remaining-stage cadence and commit-boundary provisions of the T2 packet revision v2 | — | Binding for the consolidation of the remaining M3.2 T2 implementation stages and the replacement implementation-freeze boundary. Formal outcome **`M3_2_REMAINING_STAGES_COMBINED`**. **Governance only — staging and review cadence only**: **no executable byte changes with this record**; it edits no accepted decision (032–036 untouched), no packet, no review artifact, no migration, no template, and no `Docs/decision_index.md`. **It does not authorize implementation of T2.2–T2.3** or any later stage, nor any executable edit, `m3/acquisition.py`, operational catalog, storage integration, scripted or live transport, receipt emission, network or CompanyFacts enablement, SEC connectivity testing, live SEC access, acquisition, ceiling-801 use, T3/T4/T5 or Gate H execution, migration, receipt-schema change, new reason code, or tag. **Next action: `CHATGPT_PREPARATION_OF_COMBINED_M3_2_T2_2_T2_3_IMPLEMENTATION_PACKET`** |
| 038 | [M3.2 T2.2–T2.3 Implementation-Path Envelope Amendment](decision_038_m3_2_t2_2_t2_3_path_envelope_amendment.md) (records verbatim the owner's 2026-08-05 instrument `OWNER_DECISION_038_M3_2_T2_2_T2_3_PATH_ENVELOPE_AMENDMENT: APPROVED`; adopts the technical findings of the fresh independent no-subagent adversarial rereview of the second-corrected combined T2.2–T2.3 candidate `6b189df1651ec3674ec7f96a1f5d66f488c654a9` (tree `8850e1e45e9471bbb8b94612da67715e932a496f`, published parent `feb9e134307a9551475f243dc0c1ddcecc89ffde`, subject `Implement M3.2 T2.2-T2.3 acquisition foundation`), whose verdict was `M3_2_T2_2_T2_3_SECOND_CORRECTED_INDEPENDENT_REREVIEW: PASS_WITH_REQUIRED_CORRECTIONS` with **zero BLOCKER** — all three prior MAJOR findings fully corrected, the candidate-owned archive-lineage phase proved memory-bounded by measurement (3,211,570-byte heap peak against a 71,303,168-byte expansion across 68 members: 3.06× the largest member, 4.5 % of total expanded content, at most 2 members simultaneously live), observation-member persistence proved transactional and deterministic across 12 rollback and reuse-boundary probes, fixtures proved clock-independent across five timezones and five processes with 15 consecutive clean runs of the previously flaky classes, bounded operational outputs proved free of private-path and payload leakage across five injected failure classes, no plan/route/ceiling/completion/receipt/no-network bypass, all validation green (ruff, format, mypy strict over 76 files, targeted 272 passed, full suite **2938 passed / 1 skipped twice consecutively**), and all twelve required mutation checks load-bearing; the owner **determines every technical PASS condition satisfied** and identifies the **sole PASS-blocking issue as the absence of a durable path-envelope amendment**, a governance-record defect explicitly requiring **no code change**. **Amends — for the combined T2.2–T2.3 stage only** — Decision 035 §6, the T2 packet §5 fifteen-path maximum, and the unchanged-envelope statements in Decisions 036 and 037, by adding exactly two paths: `src/disclosure_drift/sec/observation_catalog.py` (widen `ObservationRecorder.record(..., members=...)` from an eager sequence boundary to a compatible single-pass iterable boundary and consume archive-member lineage lazily inside the observation's own transaction, preserving existing sequence-caller compatibility and deterministic order, lineage validation, rollback, reuse, and supersession behaviour) and `tests/unit/test_observation_catalog.py` (the direct tests proving those properties); the amendment is **bound to and limited by the exact changes in candidate `6b189df1…`/tree `8850e1e4…`**. Records the envelope conflict it resolves — the accepted architecture could not be implemented safely inside the original envelope, since satisfying the eager `members` boundary would have required materializing every archive member at once — and **ratifies the earlier explicit owner correction authorization of 2026-08-05 granted before the paths were edited**, stating expressly that this is **not retrospective self-widening by an implementation agent** but the durable record of a prior owner adjudication. **Decision 038 is higher authority** for this narrow point; the supersession is **partial and narrow**, leaving the envelope's ceiling-not-grant character, the immediate-stop rule for any further out-of-subset need, the stage cadence, the commit boundaries, the no-stage-tag and no-T3-tag rules, and every other declined and prohibited surface in force. **Edits no accepted decision in place** (032–037 byte-unchanged); the T2 authorization packet is **preserved byte-identical** at SHA-256 `621201464ffd0e236b90aefe3cd9f587b1c4873011e32df2aef596c7ff314599` and **must not be silently rewritten**; the accepted contract is **not edited** and retains SHA-256 `c526335b91ddb75877e66ecef3255dce6c4c27e60ae0c5a7286228935d42edb7`) | 2026-08-05 | **ACCEPTED — OWNER APPROVED 2026-08-05** | Decision 035 §6, T2 packet §5, and the Decision 036/037 unchanged-envelope statements — **only** as to the two named paths for the combined T2.2–T2.3 stage | — | Binding for the authorized implementation-path envelope of the combined M3.2 stage T2.2–T2.3, and nothing else. Formal outcome **`M3_2_T2_2_T2_3_PATH_ENVELOPE_AMENDMENT_RECORDED`**. **Governance only**: **no executable byte changes with this record**; it edits no accepted decision, no contract, no packet, no review artifact, no migration, no template, no configuration, no reason code, no receipt schema, and no `Docs/decision_index.md`. It authorizes **no further edit** to either added path beyond candidate tree `8850e1e4…`, adds neither path to T2.4 or T2.5–T2.6 authority, broadens no other stage envelope, and grants **no** operator CLI wiring, T2.4, T3/T4/T5, network or CompanyFacts enablement, SEC connectivity testing, live SEC access, acquisition, receipt or evidence creation, operational-catalog creation, use of ceiling 801, Gate H, tag, or push. **It neither accepts nor publishes the implementation candidate**, which remains local, unaccepted, and unpushed; acceptance and push remain separate owner acts. **Next action: `CHATGPT_OWNER_ACCEPTANCE_AND_PUSH_DECISION_FOR_M3_2_T2_2_T2_3_AFTER_DECISION_038_RECORDING`** |
| 039 | [M3.2 Stage T2.2–T2.3 Acceptance and Publication Authorization](decision_039_m3_2_t2_2_t2_3_stage_acceptance.md) (records verbatim the owner's 2026-08-06 instrument `OWNER_DECISION_039_M3_2_T2_2_T2_3_STAGE_ACCEPTANCE_AND_PUBLICATION_AUTHORIZATION: APPROVED`; **accepts the combined Milestone 3.2 implementation stage T2.2–T2.3 — Catalog, Immutable Storage, and Acquisition Engine** — at candidate `6b189df1651ec3674ec7f96a1f5d66f488c654a9` (tree `8850e1e45e9471bbb8b94612da67715e932a496f`, published baseline and parent `feb9e134307a9551475f243dc0c1ddcecc89ffde`, subject `Implement M3.2 T2.2-T2.3 acquisition foundation`, exactly six paths); adopts the final independent no-subagent adversarial technical rereview's ten findings — every technical PASS condition met; archive transport and candidate-owned archive-member lineage **memory-bounded**; archive-member persistence **single-pass, deterministic, and transactional**; failed member enumeration or insertion leaving **no partial catalog transaction**; correct archive reuse, supersession, immutable-object preservation, and lineage reconciliation; ZIP fixtures deterministic and independent of wall clock, locale, timezone, and building platform; bounded operational-error outputs disclosing no private path or payload; request-plan, route, ceiling, completion, recovery-observability, and no-network boundaries **fail-closed**; required static, targeted, mutation, determinism, and full-suite validation passed; and **no live SEC access, operational catalog, receipt, evidence artifact, or ceiling usage** — the rereview having withheld a clean PASS **solely** because two implementation paths lay outside the last durably recorded envelope; records that **accepted [Decision 038](decision_038_m3_2_t2_2_t2_3_path_envelope_amendment.md) resolved that governance-record defect** by narrowly authorizing `src/disclosure_drift/sec/observation_catalog.py` and `tests/unit/test_observation_catalog.py`, and accepts Decision 038 as the **controlling higher-authority amendment** for those paths and purposes; classifies the stage **`M3_2_T2_2_T2_3_ACCEPTED_AND_COMPLETE`**; and **authorizes one normal fast-forward push** of `main` publishing, in order, the candidate `6b189df1…`, the Decision 038 commit `27842965ed5a8fcccbf5fbb3c3c63ff2c2e798ba` (tree `6bead61920ad947d35b300e9d81634ca5c767358`), and the Decision 039 acceptance commit — permitted only after Decision 039 is durably recorded, the registry and ledger agree, candidate bytes are unchanged, the contract and packet hashes are unchanged, `origin/main` is verified an **ancestor** of local `HEAD`, the branch is behind by zero with no divergence, and governance validation passes. **No tag is authorized for this stage.** Keeps three determinations distinct — **stage acceptance**, **publication of that stage**, and **overall M3.2 T3 implementation acceptance, which has NOT occurred** — and leaves seven obligations open: singleton bootstrap-absence reason authority before the T2.4 absence enumeration; catalog-authoritative adoption after quarantine at T2.4; conditional-request and 304/cache-resume handling at T2.4; the accepted `RawStore` resource limitation as a T4 preflight and integrated-candidate-review concern; sanitization or exclusion of untrusted progress-sink messages before any later receipt or indexed-artifact use; F4 evidence-index vocabulary resolution no later than T4; and D023-O1 as a latent fail-closed referral condition) | 2026-08-06 | **ACCEPTED — OWNER APPROVED 2026-08-06** | — | — | Binding for the acceptance and publication of M3.2 implementation stage T2.2–T2.3 only. Formal outcome **`M3_2_T2_2_T2_3_ACCEPTED_AND_COMPLETE`**. **Governance only**: **no executable byte changes with this record**; it edits no accepted decision (032–038 byte-unchanged), no contract, no packet, no review artifact, no migration, no template, no configuration, no reason code, no receipt schema, and no `Docs/decision_index.md`. It is **not** overall M3.2 T3 implementation acceptance. It grants **no** T2.4 authority and does **not** carry the Decision 038 path expansion into T2.4 or T2.5–T2.6; and grants no repair, reconciliation, drift inspection, or resume; no operator CLI wiring; no conditional requests, 304 handling, or cache resume; no singleton bootstrap-absence reason-code resolution; no F4 resolution; no D023-O1 modification; no network or CompanyFacts enablement; no SEC contact or connectivity testing; no real operational-catalog creation; no receipts, private evidence, acquisition, or use of ceiling 801; and no tag, force push, history rewrite, rebase, squash, or amend. **Next action: `CHATGPT_OWNER_M3_2_T2_4_IMPLEMENTATION_AUTHORIZATION_AFTER_T2_2_T2_3_PUBLICATION`** |
| 040 | [M3.2 T2.4 Implementation Authorization](decision_040_m3_2_t2_4_implementation_authorization.md) (records verbatim the owner's 2026-08-06 instrument `OWNER_DECISION_040_M3_2_T2_4_IMPLEMENTATION_AUTHORIZATION: APPROVED`; **accepts the read-only T2.4 architecture and implementation-packet discovery outcome `M3_2_T2_4_IMPLEMENTATION_PACKET_DISCOVERY_COMPLETE`** and **authorizes Milestone 3.2 stage T2.4 — Recovery, Reconciliation, Resume Boundaries, and Drift Control** as one coherent implementation stage with four internal subphases: **T2.4-A** catalog-authoritative reconstruction (open the accepted operational catalog; load durable observations in deterministic order; construct a fresh `SnapshotStore` and adopt catalog observations into it; discard all predecessor-process mutable in-memory snapshot state; exclude quarantined, failed, absent, missing, unverifiable, or otherwise unusable observations from reuse; preserve lawful predecessor, supersession, duplicate, validator, and archive-lineage identities; re-verify immutable evidence at the point of reuse — the catalog remains the durable source of truth); **T2.4-B** deterministic, read-only reconciliation and drift inspection over plan, catalog observations and reasons, immutable objects, archive-member lineage, attempt accounting, predecessor chains, staging and raw partials, orphans and missing referents, request and route identities, completion state, and registered drift reasons — enumerating item-level state in deterministic plan order across seventeen distinguished states, mutating nothing; **T2.4-C** a deterministic, read-only continuation proposal bound to predecessor receipt-chain identity, exact plan hash, exact acquisition window, exact approved ceiling, exact catalog and object state, and cumulative attempt evidence — cumulative consumption without reset; refusal of consumed attempts beyond the approved ceiling; conservative full-`A_reachable` charge for at most one identifiable receiptless in-flight request; `UNDETERMINED` classification for uncertain attempt attribution with continuation prohibited; exact remaining headroom and exact satisfied/remaining logical requests; no replay of verified satisfied requests; ETag and Last-Modified only from a lawful verified predecessor; a 304 satisfying only through accepted immutable-evidence and lineage verification and an unreconciled 304 failing closed with `SOURCE_SNAPSHOT_REUSE_UNRECONCILED`; byte-identical 200 preserved as duplicate reconciliation; changed 200 preserved as a new immutable superseding observation; continuation refused when the worst-case remaining attempt bound does not fit — a continuation **proposal only**, with continuation authorization an explicit later owner act and execution plus `m3 acquire --resume-from` wiring deferred; and **T2.4-D** an inert explicit recovery-action library boundary supporting only four deterministic action classes (adopt one cryptographically proven orphan through the accepted authoritative reconciliation path; quarantine one identified partial raw object through the accepted move-and-preserve path; remove one identified stale staging `.part` spool proven never promoted, never catalogued, and never referenced; rebuild the derived audit projection through the accepted projection-rebuild primitive) — never automatic, one explicitly requested action re-verified immediately before acting, refusing mismatched/stale/already-resolved/multi-action/`UNDETERMINED` requests, deleting or overwriting no acquired object, recording through the accepted recovery-event surface, requiring a fresh read-only inspection after mutation, treating mutation-then-failed-event-recording as `UNDETERMINED`, and exposed by **no CLI during T2.4**. **Approves exactly one new registered reason code `SOURCE_REQUIRED_OBJECT_UNAVAILABLE`** (integrity; `blocks_release` true; `requires_manual_review` true; decision reference this record; attached to a required, non-quarterly-index M3.2A logical request whose committed observation is terminally failed or quarantined; may coexist with a more specific accepted defect code; quarterly-index instances retain `INDEX_INSTANCE_UNAVAILABLE` and related codes; stopped/interrupted/ceiling-exhausted/not-attempted requests do not receive it merely for lacking an object; recent-target 404 semantics unchanged; no existing reason redefined or aliased; **no second new code**; no M3.2B mapping). **Fixes `NO_NEW_MIGRATION_REQUIRED`** (chain exactly `0001`–`0013`; the code is runtime-seeded through the accepted registry) **and `NO_RECEIPT_SCHEMA_CHANGE_REQUIRED`** (`m3-execution-receipt/2.0` frozen; T2.4 emits no receipt, changes no field or completion-status vocabulary, places no item-level reconciliation identities in a receipt, and preserves unknown-field refusal and registered-reason validation); names the durable reconciliation source (`census_source_observations`, `census_observation_reasons`, `census_archive_members`, the accepted recovery-state and recovery-event tables where applicable, and the governed immutable-object and staging trees — the earlier packet's `census_index_instances` sketch does not override the tables the accepted T2.2–T2.3 implementation actually writes); fixes the accounting-vocabulary ruling (already-satisfied-and-excluded → the future receipt's `cache_hit_count`; lawful 304 → the future `not_modified_count`; byte-identical 200 → the future `duplicate_object_count`; the existing `WindowOutcome.cache_hits` name must not populate `cache_hit_count`) and the three-part cumulative attempt-accounting formula (resolvable predecessor-chain count, plus deterministically attributable post-receipt committed-observation attempts, plus full registered `A_reachable` for at most one identifiable receiptless in-flight request; no segment counted twice; `UNDETERMINED` on ambiguity; the ceiling never increased, reset, shadowed, reinterpreted, or replaced); **rules F4 non-blocking for T2.4** (due no later than T4 and before the first affected freeze or reconciliation artifact is publicly indexed); and **fixes the exact eight-path maximum implementation envelope** — production `src/disclosure_drift/m3/acquisition.py`, `src/disclosure_drift/m3/recovery.py`, `src/disclosure_drift/m3/__init__.py`, `src/disclosure_drift/reasons.py`; tests `tests/unit/test_m3_acquisition.py`, `tests/unit/test_m3_recover.py` (the one authorized new test file), `tests/unit/test_m3_recovery.py`, `tests/unit/test_reasons.py` — **expressly adding `tests/unit/test_reasons.py` to the Decision 035 path envelope for T2.4 only** (the one-code registry delta, exact closed-set count adjustment, and metadata and registration assertions), a narrow, stage-scoped higher-authority amendment; the historical T2 packet remains byte-identical; **Decision 038 has no authority over T2.4**; any need for another path is an immediate stop before that path is touched. Fixes the commit-and-review rule — at most one implementation commit with exact subject `Implement M3.2 T2.4 recovery and reconciliation`, local until implementation completion, ChatGPT owner review, one fresh independent no-subagent stage audit, correction and rereview where required, and separate owner acceptance and publication authorization; no interim commit without a new owner ruling; **no T2.4 stage tag**; **T2.5–T2.6 may not begin until T2.4 is accepted and published** — the §16 validation rule (one full-suite run with the SEC extra at normal completion; every protected path proven unchanged), the eighteen §17 stop conditions, and the §19 continuing obligations) | 2026-08-06 | **ACCEPTED — OWNER APPROVED 2026-08-06** | Decision 035 §6's fifteen-path maximum and the T2 packet §5 table — **only** as to the eight-path T2.4 envelope and the added `tests/unit/test_reasons.py`, for stage T2.4 only | — | Binding for the implementation authorization, internal subphases, reason-code approval, migration/receipt/accounting/F4 dispositions, exact path envelope, and commit/review/validation/stop rules of M3.2 stage T2.4, and nothing else. Formal outcome **`M3_2_T2_4_IMPLEMENTATION_AUTHORIZED`**. **Governance only**: **no executable byte changes with this record** — the reason-registry delta is implemented only later, inside the authorized T2.4 stage; it edits no accepted decision, contract, packet, review artifact, migration, template, configuration, or `Docs/decision_index.md`. **Its §18 withholds implementation until the separate exact T2.4 implementation packet is issued**, and it grants no operator CLI wiring, receipt emission, reconciliation-report creation, evidence indexing, real operational catalog, live SEC access, connectivity testing, network or CompanyFacts enablement, ceiling-801 use, T2.5–T2.6, T3/T4/T5/Gate H/M3.3 work, migration, receipt-schema change, second reason code, further path, tag, force push, or history rewrite. **Next action: `CHATGPT_OWNER_ISSUANCE_OF_M3_2_T2_4_IMPLEMENTATION_PACKET_AFTER_DECISION_040_PUBLICATION`** |
| 041 | [M3.2 T2.4 Recovery-State Primitive and Path-Envelope Amendment](decision_041_m3_2_t2_4_recovery_state_primitive_authority.md) (records verbatim the owner's 2026-08-06 instrument `OWNER_DECISION_041_M3_2_T2_4_RECOVERY_STATE_PRIMITIVE_AUTHORITY: APPROVED`; **accepts the read-only durable-lifecycle feasibility outcome `M3_2_T2_4_DURABLE_RECOVERY_LIFECYCLE_NOT_FEASIBLE_WITHIN_CURRENT_AUTHORITY`** on seven grounds — no accepted callable resolves an exact generic `census_recovery_states` row; the sole existing resolver is embedded in `rebuild_audit_projection`; it is hard-filtered to `audit_projection_interrupted`; it resolves every blocked projection state for a run rather than one exact primary-key identity; it performs a projection rebuild and other unrelated mutations; it resolves during the mutation, before recovery-event recording; and three of the four T2.4 recovery actions have no resolver at all — determining that **the schema is sufficient and the missing capability is an accepted exact primitive**, and accepting the feasibility session's use of Claude Opus 5 rather than Claude Fable 5 and the independent-audit report's absence from that session as non-material and nonblocking. **Amends the Decision 040 §11 eight-path T2.4 envelope to exactly ten tracked paths** for the T2.4 correction only, adding `src/disclosure_drift/sec/observation_catalog.py` and `tests/unit/test_observation_catalog.py`, and **amends Decision 040 §12 only to release `observation_catalog.py`** for the narrow additions authorized here — no other previously prohibited path is released, the ten paths are a maximum rather than a requirement to edit every path, and an eleventh path is an immediate stop. **Authorizes exactly two additive public recovery-state primitives** in `observation_catalog.py`: `open_recovery_state` (nonempty inputs required; verifies `census_run_id` identifies an existing `ops_ingestion_jobs.job_id`; inserts exactly one `census_recovery_states` row with `resolution_state = 'blocked'` addressed by the full primary key `(census_run_id, recovery_state_id)`; commits through accepted transaction and writer-lease conventions; raises on missing run, duplicate state identity, constraint failure, or failed write; writes nothing to `census_recovery_events` or `census_projection_recovery_events`; mutates no observation, object, projection, receipt, or other recovery state; **no silent skip when a run ID is absent**) and `resolve_recovery_state` (updates only the exact primary-key row; requires its current state blocked; sets `resolution_state = 'resolved'`; records the completed action and sanitized detail; performs no scenario filtering, no projection rebuild, and no repair; updates no sibling state; writes no recovery-event row; returns success only when exactly one blocked row was resolved; treats zero affected rows as failure; **must not bulk-resolve by run or scenario**) — with existing public and private functions retaining their accepted semantics and no existing resolver, reconciliation function, recorder, schema, or projection behavior rewritten to simulate the new authority. **Fixes the generic state scenario `t2_4_recovery_action`**, stored only in `census_recovery_states` and never inserted into the CHECK-constrained `census_recovery_events`. **Fixes the run-identity ruling** — every mutating T2.4 recovery action requires a caller-supplied, already registered `ops_ingestion_jobs.job_id` as `census_run_id`, with pre-mutation refusal when it is absent, empty, unresolvable, not a lawful existing governed run, or when a blocked T2.4 state already exists for that run; T2.4 may not create an ingestion-job row, invoke the private census-orchestrator job creator, fabricate a job identity, substitute a receipt ID, create a new recovery-run identity model, or create a real operational run during tests or implementation, though tests may create lawful temporary catalog fixtures; the recovery-state identity is an opaque unique ID for which UUID generation is permitted and which is **not** a plan, evidence, receipt, request, or authorization identity. **Fixes the corrected thirteen-step write-ahead sequence** — block committed and fresh-connection verified before mutation; exactly one authorized mutation; the actual event recorded through `record_recovery_events` with `census_run_id=None` so no second recovery-state row is created; exact resolution **only after** event recording succeeds; fresh-connection resolution verification; and a fresh read-only recovery inspection required before continuation — with **opening the block expressly not itself a recovery event**. **Fixes eight failure outcomes** (refuse before mutation on run-validation, open, or blocked-state-verification failure; leave the exact state blocked on mutation, event-recording, or resolution failure; an unresolved block after restart makes fresh inspection report a blocking state and prohibits continuation; a second action while a block remains unresolved is refused before mutation), rules a committed resolution whose readback cannot complete `UNDETERMINED` for that invocation, and fixes that **no in-memory flag may be the only continuation prohibition**. Fixes fifteen required primitive tests with `tests/unit/test_observation_catalog.py` editable **only** to prove the new pair and existing tests remaining load-bearing. Leaves unchanged `NO_NEW_MIGRATION_REQUIRED` (chain exactly `0001`–`0013`), `NO_RECEIPT_SCHEMA_CHANGE_REQUIRED` (`m3-execution-receipt/2.0` frozen), exactly one T2.4 reason-code addition, no alias, no route or source-authority change, and no configuration change. **Rules on the candidate and unpublished history** — the candidate `5cba2863f47df09c83564258be897a4fd71cf6be` (tree `e3c47528e6059c7b8e10369846934c56e3b8eabe`) remains local, unaccepted, unpushed, and untagged and may not be pushed; this record is recorded and published **from a disposable governance clone without changing the primary checkout**; a separate later correction packet may authorize the primary checkout to fetch the new baseline, verify the candidate unchanged, preserve its code delta, move local `main` to the published Decision 041 baseline through **one controlled soft reset**, apply the corrections, and create **exactly one** corrected implementation commit with the subject `Implement M3.2 T2.4 recovery and reconciliation`, with the old unaccepted SHA **not** preserved by a branch or tag, the corrected implementation local until a fresh independent rereview passes and the owner separately accepts publication, and **no authority to change published history**. Requires the correction to still resolve nine sustained independent-audit findings — durable post-mutation fail-closed behavior; exact durable in-flight identity or `UNDETERMINED` classification; preservation of the multiple-possible-in-flight basis; exhaustive continuation-state partitioning; blocking of hash mismatch and invalid archive lineage; stray-lineage adoption coverage; symlink-sweep alignment; refusal-reason coverage; and snapshot-counter documentation — with mutation 02 accepted as a proven no-op) | 2026-08-06 | **ACCEPTED — OWNER APPROVED 2026-08-06** | Decision 040 §11's eight-path maximum and §12's prohibited-path set — **only** as to the two added paths and the narrow additive primitives, for the T2.4 correction only | — | Binding for the feasibility disposition, the T2.4 path-envelope amendment to ten paths, the exact additive recovery-state primitive pair, the `t2_4_recovery_action` vocabulary, the run-identity ruling, the corrected write-ahead sequence and failure semantics, the primitive test requirements, the candidate-correction and unpublished-history ruling, and the continuing correction obligations, and nothing else. Formal outcome **`M3_2_T2_4_RECOVERY_STATE_PRIMITIVE_AUTHORITY_RECORDED`**. **Governance only**: **no executable byte changes with this record** — the primitive pair is implemented only later, inside the separately issued T2.4 correction packet; it edits no accepted decision (032–040 byte-unchanged), no contract, no packet, no review artifact, no migration, no template, no configuration, no reason code, no receipt schema, and no `Docs/decision_index.md`. **Decision 038 has no authority over T2.4** and is not the source of this record's path release. It grants no correction before this record is durably published, and no migration, receipt change, second reason code, eleventh path, direct recovery-state SQL from `m3/acquisition.py`, private-catalog-helper use, reuse of the projection-specific bulk resolver, operational-run creation, CLI wiring, operational catalog, network or CompanyFacts enablement, SEC contact, push of the T2.4 implementation, tag, T2.5–T2.6, T3/T4/T5/Gate H/M3.3 work, or repository-efficiency or review-infrastructure work. **Next action: `CHATGPT_OWNER_REISSUANCE_OF_M3_2_T2_4_CORRECTION_PACKET_AFTER_DECISION_041_PUBLICATION`** |
| 042 | [M3.2 Stage T2.4 Acceptance and Publication](decision_042_m3_2_t2_4_acceptance_and_publication.md) (records the owner's 2026-08-06 acceptance-and-publication ruling for Milestone 3.2 implementation stage **T2.4 — Recovery, Reconciliation, Resume Boundaries, and Drift Control**. **Accepts the fresh independent corrected-candidate rereview verdict `M3_2_T2_4_CORRECTED_CANDIDATE_REREVIEW_PASS`** — Claude Opus 5, Max effort, fresh independent session, **zero BLOCKER, zero MAJOR, zero MINOR**, mandatory separate-OS-process durability challenge PASS, independent mutation campaign **18/18 killed**, targeted validation **333 passed**, full suite **3053 passed / 1 pre-existing intentional skip** (the long-standing `[sec]`-extra skip), static gates clean; the rereview reached the repository through the owner's supplied acceptance evidence and **no rereview artifact file was previously recorded here, so none is created, reconstructed, back-dated, or assigned a path or SHA-256**. **Accepts the corrected T2.4 candidate** `625c03d6931e01acc99946ca3924f1cda4da6b76` (tree `816fd392df859106b9ba21b684f9b4a8061461fc`; parent and Decision 041 governance baseline `4897bb1d8fc5be5cd6d12be941204e377bbfa5a4`; subject `Implement M3.2 T2.4 recovery and reconciliation`), which changes exactly **eight** tracked paths — `src/disclosure_drift/m3/acquisition.py`, `src/disclosure_drift/m3/__init__.py`, `src/disclosure_drift/reasons.py`, `src/disclosure_drift/sec/observation_catalog.py`, `tests/unit/test_m3_acquisition.py`, `tests/unit/test_m3_recover.py`, `tests/unit/test_observation_catalog.py`, `tests/unit/test_reasons.py` — all inside the ten-path Decision 041 §4 maximum with **no eleventh path**, leaving `src/disclosure_drift/m3/recovery.py` and `tests/unit/test_m3_recovery.py` unedited as that maximum permits, and changing no migration, receipt-schema, configuration, contract, packet, decision, script, template, CI, or documentation byte. **Accepts Decision 041's recovery-state primitive implementation as satisfying the authorized corrective design** — the additive public pair `open_recovery_state` and `resolve_recovery_state`, the generic `t2_4_recovery_action` vocabulary, the caller-supplied already-registered `ops_ingestion_jobs.job_id` run-identity ruling, the corrected thirteen-step write-ahead sequence, and the eight fixed failure outcomes — and **accepts T2.4 as complete and accepted**, classification `M3_2_T2_4_ACCEPTED_AND_COMPLETE`. **Authorizes one normal fast-forward push** publishing, in order, the accepted candidate and this acceptance commit (exact subject `Accept and publish M3.2 T2.4`), with the candidate **not rewritten, amended, squashed, rebased, reset, or cherry-picked** and the governance change created on top of it, permitted only after durable recording, registry and ledger agreement, unchanged candidate bytes, unchanged contract and packet hashes, a staged path set of exactly the three authorized governance paths, and passing governance validation; **no tag, no release, no force push, no `--force-with-lease`, no history rewrite, rebase, squash, or amend**. Keeps stage acceptance, publication, and **overall M3.2 T3 implementation acceptance — which has NOT occurred** — distinct, and carries forward unchanged the six Decision 040 §19 obligations: the accepted `RawStore` resource limitation as a T4 concern; sanitization or exclusion of untrusted progress-sink messages; F4 no later than T4; D023-O1 as a latent fail-closed referral condition; operator wiring and receipt assembly during T2.5–T2.6; and overall independent T3 acceptance after the combined T2.5–T2.6 freeze candidate) | 2026-08-06 | **ACCEPTED — OWNER APPROVED 2026-08-06** | — | — | Binding for the acceptance of M3.2 stage T2.4 at candidate `625c03d6…`, the acceptance of the corrected-candidate rereview, the acceptance of the Decision 041 primitive implementation as satisfying the corrective design, and the publication of that candidate — and nothing else. Formal outcome **`M3_2_T2_4_ACCEPTED_AND_PUBLISHED`**. **Governance only**: **no executable byte changes with this record**; it edits no accepted decision (032–041 byte-unchanged), no contract, no packet, no review artifact, no migration, no template, no configuration, no reason code, no receipt schema, and no `Docs/decision_index.md`. **T2.4 acceptance does not itself grant T2.5, T2.6, network, operational-catalog, live-SEC, or 801-ceiling execution authority**: it authorizes no T2.5 or T2.6 implementation, does not begin T2.5, enables neither `network.enabled` nor `network.m3_acquire_enabled` nor CompanyFacts, and grants no SEC contact, connectivity testing, live request, live acquisition, real operational catalog, operational `m3 acquire` execution, receipt emission, private reconciliation-report creation, evidence indexing, ceiling-801 use, M3.2B activity, any T3/T4/T5 authority not already separately granted, Gate H or M3.3 work, migration, receipt-schema change, further reason code, eleventh path, or tag. **Next action: `CHATGPT_OWNER_M3_2_T2_5_STAGE_AUTHORIZATION_DECISION`** |
| 043 | [M3.2 G1 Navigation and Workflow Repair Authorization](decision_043_m3_2_g1_navigation_workflow_repair_authorization.md) (records the owner's 2026-08-06 instrument `OWNER_DECISION_043_M3_2_G1_NAVIGATION_AND_WORKFLOW_REPAIR_AUTHORIZATION: APPROVED`; accepts the read-only post-T2.4 workflow-efficiency discovery recommendation `RECOMMEND_MINIMAL_OPTIMIZATION_BEFORE_T2_5` and authorizes one bounded **non-production** stage, **M3.2 G1 — Navigation and Workflow Repair**, that sits **outside the contract T-series** and alters no T2 cadence, completion, or methodology. Fixes a **hard semantic boundary** — G1 may alter no SEC acquisition, planning, route/source, recovery, reconciliation, attempt-accounting, ceiling, receipt, reason-code, schema, migration, network-configuration, methodology, or contract meaning, and **no production source or test behavior may change**. Fixes the **seven-path implementation envelope as a ceiling** — `Docs/decision_index.md`, `Docs/change_impact_map.md`, `Docs/architecture_map.md`, `Milestones/STATUS.md`, `scripts/context_snapshot.sh`, `Makefile`, and the new `Docs/m3/review_execution_conventions.md` — with **no eighth path**. Fixes the five work items: **R1** navigation aids brought current through Decision 042 with concise pointers and no second authority source; **R2** shortening only the overgrown machine markers (`CURRENT_STAGE`, `ACTIVE_BLOCKER`, `IMPLEMENTATION_AUTHORIZATION`, ~200 characters each) with **no structural rewrite and no deletion of audit-relevant fact**, correcting the stale skip and active-contract prose, and improving the **existing** context tool with HEAD tree, parent, ahead/behind, and both network switches plus a continuation-line marker-parsing fix, while **prohibiting** any hash or authority manifest in that script; **R3** a `make stage-gate` running `make check`, `make sqlite-check`, and `make context` in deterministic sequential order, itself **not authority**; **R4** a subordinate review-execution conventions document covering session preflight with STOP-by-default on role/model/freshness mismatch, authority/execution/evidence separation, packet and report compression, a reviewer-owned isolated-clone procedure with mandatory verified teardown and **no repo-owned audit oracle**, mutation hygiene with the `KILLED`/`SURVIVED_EFFECTIVE`/`SURVIVED_NO_OP` classification and **no mutation framework**, and validation tiers; and **R5** a **prospective** durable-independent-review-artifact lifecycle piloted on G1, expressly **forbidding** reconstruction or back-dating of the missing historical T2.2–T2.3 and T2.4 artifacts. Supersedes the Decision 033 §5 navigation-preservation instruction **partially and prospectively**, solely to permit this repair, leaving historical decisions immutable and navigation aids non-authoritative. Records three historical observations **without editing Decision 042** — the accepted T2.4 single skip was the fixed-literal `test_m23_pilot_manifest.py` skip while the HTTPX transport tests executed, so Decision 042's `[sec]`-extra wording **understated** actual validation, and T2.4 acceptance is unaffected. Explicitly **rejects or defers** a repo-owned audit harness, a second context command, a mutation framework, a frozen-input manifest, a `STATUS.md` structural rewrite, an `m3/acquisition.py` split, and any production refactor. Authorizes **at most one** G1 implementation commit, subject `Repair M3.2 navigation and review workflow`, **no tag**, **no T2.5 work**, and requires a **separate** fresh independent-review authorization afterward, with G1 acceptance and T2.5 authorization remaining separate owner judgments. **Governance only — no executable byte changes with this record, and G1 has not begun**; it grants no T2.5, T2.6, network, CompanyFacts, live-SEC, operational-catalog, ceiling-801, migration, receipt, reason-code, production-behavior, tag, T3/T4/T5, Gate H, or M3.3+ authority. **Next action: `CHATGPT_OWNER_ISSUANCE_OF_M3_2_G1_IMPLEMENTATION_PACKET_AFTER_DECISION_043_PUBLICATION`** |

## Controlling record by topic (quick lookup)

| Topic | Controlling record |
|---|---|
| **The M3.2 G1 non-production stage: its authorization, seven-path envelope, R1–R5 content, the partial supersession of the Decision 033 §5 navigation-preservation instruction, the prospective durable-review-artifact lifecycle, and the rejected optimizations** | **Decision 043 — `ACCEPTED — OWNER APPROVED 2026-08-06`; binding.** The controlling record for **whether M3.2 G1 is authorized** (it is — `M3_2_G1_NAVIGATION_AND_WORKFLOW_REPAIR_AUTHORIZED`), **which seven paths it may touch** (a ceiling, no eighth), **what it may not alter** (§4's hard semantic boundary; no production source or test behavior), and **that it has not begun** — G1 implementation requires the separate owner-issued G1 implementation packet. It is **not** authority for T2.5, T2.6, network, live SEC, or any production change, and it does not edit Decisions 001–042. |
| **M3.2 stage T2.4 implementation authorization, the T2.4 eight-path envelope and its `tests/unit/test_reasons.py` addition, the `SOURCE_REQUIRED_OBJECT_UNAVAILABLE` reason-code approval, and the T2.4 migration, receipt, accounting-vocabulary, attempt-accounting, and F4 dispositions** | **Decision 040 — `ACCEPTED — OWNER APPROVED 2026-08-06`; binding.** The controlling record for **whether stage T2.4 is authorized** (it is — `M3_2_T2_4_IMPLEMENTATION_AUTHORIZED`, subject to its instrument §18: **no implementation before the separate exact T2.4 implementation packet is issued**), for the verbatim owner instrument and its acceptance of the discovery outcome `M3_2_T2_4_IMPLEMENTATION_PACKET_DISCOVERY_COMPLETE`, for the four internal subphases T2.4-A through T2.4-D and the no-CLI-exposure rule, for the **one approved reason code** `SOURCE_REQUIRED_OBJECT_UNAVAILABLE` with its exact mapping and exclusions, for `NO_NEW_MIGRATION_REQUIRED` and `NO_RECEIPT_SCHEMA_CHANGE_REQUIRED`, for the durable reconciliation source tables, for the accounting-vocabulary and cumulative attempt-accounting rulings with their `UNDETERMINED` fail-closed rule, for F4 remaining non-blocking at T2.4 (due no later than T4), for the **exact eight-path maximum envelope** including the narrow stage-scoped addition of `tests/unit/test_reasons.py` to the Decision 035 envelope for T2.4 only, for the one-commit rule with exact subject `Implement M3.2 T2.4 recovery and reconciliation` and the review ladder ending in separate owner acceptance and publication, and for the stop conditions and continuing obligations. Decision 035 remains controlling for the staged T2 authorization it narrowly amends; Decision 037 for the four-stage cadence; Decision 039 for T2.2–T2.3 acceptance; **Decision 038 has no authority over T2.4**. It grants no live authority of any kind |
| **M3.2 T2.4 recovery-state primitive authority, the T2.4 ten-path envelope amendment, the `t2_4_recovery_action` vocabulary, the run-identity ruling, and the corrected write-ahead sequence** | **Decision 041 — `ACCEPTED — OWNER APPROVED 2026-08-06`; binding.** The controlling record for **whether the durable blocked-to-resolved recovery lifecycle was feasible inside the Decision 040 envelope** (it was not — `M3_2_T2_4_DURABLE_RECOVERY_LIFECYCLE_NOT_FEASIBLE_WITHIN_CURRENT_AUTHORITY`, on seven enumerated grounds, the schema being sufficient and the missing capability an accepted exact primitive), for the verbatim owner instrument, for the **amendment of the Decision 040 §11 eight-path envelope to exactly ten paths** and of §12 solely to release `src/disclosure_drift/sec/observation_catalog.py` for the narrow additions, for the **two additive public primitives** `open_recovery_state` and `resolve_recovery_state` and their exact required behaviors, for the generic `t2_4_recovery_action` state scenario stored only in `census_recovery_states`, for the **caller-supplied already-registered `ops_ingestion_jobs.job_id` run-identity ruling** and its refusal conditions, for the **corrected thirteen-step write-ahead sequence** in which exact resolution happens only after event recording succeeds and opening the block is not itself a recovery event, for the eight failure outcomes and the rule that **no in-memory flag may be the only continuation prohibition**, for the fifteen required primitive tests, for the **candidate-correction and unpublished-history ruling** (recording and publication from a disposable governance clone; a later packet's single controlled soft reset; the old unaccepted SHA not preserved by a branch or tag; no authority over published history), and for the nine continuing correction obligations. Decision 040 remains controlling for the T2.4 stage authorization, subphases, reason code, migration and receipt dispositions, commit subject, review ladder, stop conditions, and continuing obligations it fixes; Decision 035 for the staged T2 authorization; Decision 037 for the four-stage cadence; **Decision 038 has no authority over T2.4**. It grants no live authority of any kind |
| **The acceptance and publication of M3.2 implementation stage T2.4** | **Decision 042 — `ACCEPTED — OWNER APPROVED 2026-08-06`; binding.** The controlling record for **whether stage T2.4 is accepted and published** (it is — `M3_2_T2_4_ACCEPTED_AND_PUBLISHED`, stage classification `M3_2_T2_4_ACCEPTED_AND_COMPLETE`), for the accepted corrected candidate `625c03d6931e01acc99946ca3924f1cda4da6b76` / tree `816fd392df859106b9ba21b684f9b4a8061461fc` / parent `4897bb1d8fc5be5cd6d12be941204e377bbfa5a4` and its exact eight changed paths inside the Decision 041 ten-path maximum, for the accepted rereview verdict `M3_2_T2_4_CORRECTED_CANDIDATE_REREVIEW_PASS` and its recorded execution properties (with **no rereview artifact invented**, none having previously been recorded in this repository), for the acceptance of Decision 041's primitive implementation as satisfying the authorized corrective design, and for the one-normal-fast-forward-push publication with **no tag** and no rewrite of the accepted candidate. Decision 041 remains controlling for the recovery-state primitive authority, ten-path envelope, `t2_4_recovery_action` vocabulary, run-identity ruling, and corrected write-ahead sequence; Decision 040 for the T2.4 stage authorization, subphases, reason code, migration and receipt dispositions, commit subject, review ladder, stop conditions, and continuing obligations; Decision 037 for the four-stage cadence; Decision 035 for the staged T2 authorization; **Decision 038 has no authority over T2.4**. **It does not grant T2.5, T2.6, network, operational-catalog, live-SEC, or 801-ceiling execution authority, and grants no live authority of any kind** |
| **The remaining M3.2 implementation-stage structure, commit subjects, review cadence, and T3 freeze boundary** | **Decision 037 — `ACCEPTED — OWNER APPROVED 2026-08-04`; binding.** The controlling record for **how many implementation stages remain and what each contains** (three: combined T2.2–T2.3, separate T2.4, combined T2.5–T2.6), for their exact commit subjects, for the one-commit-per-stage / local-until-accepted / push-then-next cadence, for the prohibition on further combination without a new owner decision, and for the **combined T2.5–T2.6 commit as the implementation-freeze candidate** replacing the former standalone T2.6. It supersedes **only** the T2 packet's remaining-stage cadence and commit-boundary provisions — Decision 035 remains controlling for the staged T2 authorization and the fifteen-path envelope, Decision 036 for T2.1 completion, and the packet for everything else. It grants no implementation, network, catalog, ceiling, or live-SEC authority |
| **The acceptance and completion of M3.2 implementation stage T2.1** | **Decision 036 — `ACCEPTED — OWNER APPROVED 2026-08-04`; binding.** The controlling record for **whether stage T2.1 is complete** (it is — `M3_2_T2_1_ACCEPTED_AND_PUBLISHED`, at published commit `7b2ffe64…`), for both verbatim owner instruments, for the six-path delta and the byte-identity and validation evidence behind it, for the delivered fail-closed properties, for the M1/M2/O1 dispositions, and for the confirmation that **T2.2–T2.6 remain owner-gated and unauthorized**. It fixes no new rule — Decision 035 remains controlling for the staged T2 authorization, the §22 cadence, and the path envelope — and grants no implementation, network, catalog, ceiling, or live-SEC authority |
| **The staged M3.2 T2 implementation authorization, the contract §22 six-stage amendment, and the T2.1-only stage limit** | **Decision 035 — `ACCEPTED — OWNER APPROVED 2026-08-04`; binding.** The controlling record for **whether M3.2 implementation is authorized** (it is — but **only stage T2.1**, bounded to six exact paths; stages T2.2–T2.6 remain owner-gated and unauthorized), for the verbatim `APPROVED_WITH_STAGE_LIMIT` instrument and its Decision 024 §8 disposition, for the approval of T2 packet **v2** (`62120146…`) as the controlling implementation plan, for the **fifteen-path maximum envelope** and the rule that any out-of-subset need is an immediate stop for owner adjudication, for the **§22 six-stage commit and review cadence** (staging and commit governance only), for the T2.1 permitted and prohibited work, for the post-amendment contract identity `7a3fe7ff…`, for **R1** binding where applicable and **F3** binding at T2.4, for **F4 remaining unaccepted and open until no later than T4**, and for the durability condition plus the requirement that **a separate exact T2.1 execution packet precede any implementation session**. It grants no live authority of any kind; Decision 034 remains controlling for the T1 acceptance and Decision 024 §8 for the entry conditions themselves |
| **T1 acceptance of the corrected M3.2 contract, the rereview R1/R2 dispositions, and the mandatory T2-packet content** | **Decision 034 — `ACCEPTED — OWNER APPROVED 2026-08-04`; binding.** The controlling record for **whether the M3.2 contract is accepted** (it is — `M3_2_CONTRACT_ACCEPTED_AT_T1`, **T1 only**), for the verbatim owner instrument `ACCEPT_M3_2_CORRECTED_CONTRACT_AT_T1` bound to the PASS rereview (artifact SHA-256 `91235a1a…`; rereview commit `3069b03e…`; zero BLOCKER, zero MAJOR), for the accepted-text and post-acceptance contract identities (`75e7e5a1…` → `a5ac0e8d…`, status/authority metadata only), for the **R1** (MINOR, nonblocking, four-part mandatory T2-packet content) and **R2** (OPTIMIZATION, nonblocking, no contract edit) dispositions, and for the T1/T2–T5 authority separation. It grants no implementation, network, live-SEC, acquisition, catalog, or ceiling-use authority, creates no tag, and authorizes no push; Decisions 032 and 033 remain controlling for the corrections and the correction-pass adjudication |
| **The adjudication of the Decision 032 correction pass, the `Docs/decision_index.md` restoration, the exact next-action marker, and the two accepted procedural deviations** | **Decision 033 — `ACCEPTED — OWNER APPROVED 2026-08-04`; binding.** The controlling record for the owner's acceptance of the correction pass at `96dea2b…` (F1–F7 substantively addressed), for the restoration of `Docs/decision_index.md` to its `3fbaa12d…` bytes as outside the final authorized-path list — and therefore for the fact that Decision 032 §§5.5 and 7 name an F5 target that now carries no correction, with the index's stale Decision-029 sentence an open nonblocking item — for the exact marker `FRESH_NO_SUBAGENT_INDEPENDENT_REREVIEW_OF_CORRECTED_M3_2_CONTRACT`, and for the acceptance of the nonconforming published commit subject as non-substantive with **no history rewrite**. It edits neither Decision 032 nor the contract, accepts no contract, and grants no implementation, network, or live-SEC authority |
| **M3.2 contract review, the applied contract corrections, and the fresh no-subagent rereview requirement** | **Decision 032 — `ACCEPTED — OWNER APPROVED 2026-08-04`; binding.** The controlling record for the owner's adoption of the independent M3.2 contract review's findings (review verdict `M3_2_CONTRACT_INDEPENDENT_REVIEW: PASS_WITH_REQUIRED_CORRECTIONS`; artifact SHA-256 `fbf8c68c…`; review commit `3fbaa12d…`), for every correction applied to the `m3_2.md` draft (termination vs successful completion with owner-adjudicated absences; the named command-scoped network-enable change `network.m3_acquire_enabled`; the complete expected implementation/command surface; the crash-segment accounting rule; the evidence-index vocabulary gate; the sentinel gloss; the positive-control and nonchange-proof requirements), and for the procedural ruling that a **fresh independent rereview by one non-author session using no subagents** must precede owner acceptance. It accepts no contract and grants no implementation, network, or live-SEC authority |
| **M3.1 owner acceptance, its binding to the independent step-14 review, and the pre-tag M3-L11/M3-L12 dispositions** | **Decision 031 — `ACCEPTED — OWNER APPROVED 2026-08-03`; binding.** The controlling record for **whether Milestone 3.1 is owner-accepted** (it is — `M3_1_ACCEPTED_AND_COMPLETE`), for the verbatim owner acceptance instrument and its recorded acceptance reference, for the accepted evidence identities (frozen implementation `970e050…`; checklist `34fc0567…`; token record `b06ae373…`; plans `19be7bdc…`; budget `2d453e0b…`; ceiling 801), for acceptance of the step-14 review's three MINOR findings as nonblocking, and for the `CLOSURE-READY PENDING STEP 16` dispositions of M3-L11 and M3-L12 (neither closed; the committed checkpoint tag is the sole remaining criterion). It creates no tag and grants no Gate F-execution, live-SEC, or M3.2 authority — steps 16 and 17 remain separately owner-gated. Decisions 029 and 030 remain controlling for the §12 sequence and the Gate F interpretation rulings |
| **Gate F step-12 hygiene remediation, the §17 review artifact's two identities, and the owner's Gate F interpretation of the unresolved budget markers, M3-L12, and D023-O1** | **Decision 030 — `ACCEPTED — OWNER APPROVED 2026-08-03`; binding.** Controls the bounded, provably non-substantive redaction of the first durable §17 review artifact (pre-redaction `73cb1eacf0fb5e29a8a1c2ea871692068caf3ebdc48cae161d6aef677ba8f3a3` remains the historical identity; sanitized `9c40a82934ec52227202f0160d49fc5acd0e53f61af86d6f53b6e0b26e041fe3` is the current tracked identity; verdict `M3_1_SECTION_17_REVIEW: PASS` unchanged), the ruling that the three `EXACT_COUNT_RESOLVED_BY_GATE_F_ZERO_REQUEST_PLAN` response-outcome markers are permitted and nonblocking, the exact distinction "Gate-F-facing requirement satisfied; administrative closure deferred to the later M3.1 acceptance and checkpoint sequence" for M3-L12, and the `D023-O1` latent fail-closed referral classification. It signs no checklist, emits no token, and grants no network, Gate F, or M3.2 authority |
| **M3.1 rehearsal completeness, the independently tested per-route `A_reachable` witness, the M3.1A phase-token predicate, `OFFLINE_REHEARSAL_SCENARIO_MISMATCH`, and the first durable §17 review artifact** | **Decision 029 — `ACCEPTED — OWNER APPROVED 2026-08-02`; binding.** The controlling record for **whether an empty operator manifest waives the announcement route's `A_reachable` witness** (§4.1 — it does not; a zero `U(route)` never waives the witness), **what the rehearsal-only manifest-resolution fixture may and may not do** (§4), **the one new reason code and its exact metadata, including `requires_manual_review=false` as an owner ruling** (§5), **the four-predicate M3.1A token gate and the recompute-don't-trust rule for the stored-report path** (§6), **that the `A_reachable` witness must be one realizable black-box path rather than three separately measured terms** (§7), **the per-route ceiling formula and the corrected full-index count after catalog-satisfied exclusion** (§8), **the corrected fail-closed Decision 011 consequence** (§9), **the authorized paths** (§11), **the seventeen-step end-state sequence, in which `m3.1-complete` before Gate F is prohibited** (§12), and **the required content of the first durable §17 review artifact** (§13). Read §3 first if the question is what prior reviews establish — **no durable review artifact exists and none covers the current tree**. It narrowly supersedes **only** two Decision 028 clauses; Decision 028 remains controlling for everything else, and Decisions 011, 013, 024, 026, and 027 are unchanged. It **grants no implementation acceptance, no network authority, and no tag** |
| **The M3.1 readiness corrections: planner v2, corrected A1–A12, receipt v2, budget/ceiling semantics, recovery ownership, and M3-L11 protection** | **Decision 028 — `ACCEPTED — OWNER APPROVED 2026-08-01`; binding, except for the two clauses Decision 029 narrowly supersedes (§5's A6 language; §6's "exactly" with §12's closed-delta wording).** Its fresh independent rereview returned `INDEPENDENT_M3_MASTER_PLAN_REREVIEW: PASS`. It narrowly supersedes only the affected Decision 027 operational-planning language, authorizes no implementation or network access, and leaves Decisions 013 and 024 unchanged and controlling. |
| **The Milestone 3 master plan, the operational-readiness design, and the Milestone 3 sequencing, validation, model, commit, tag, and review policies** | **Decision 027 v0.2 — `ACCEPTED — OWNER APPROVED 2026-07-31`; binding.** Read its **§0 revision history** first for the eleven v0.1 corrections v0.2 applies. The controlling record for **the Milestones 0–2 closeout baseline this planning rests on** (§2), **the exact M3.1–M3.5 phase map as planned** (§5), **the M3.1A / M3.1B subdivision** (§6), **the operator-runbook requirement** (§7), **the mandatory offline rehearsal before the first SEC request** (§8), **the execution-receipt requirement** (§9), **the frozen operational-template set** (§10), **the Milestone 3 limitations register** (§11), **the sequential model and validation policy** (§12), **the commit and tag policy including the frozen future tag names** (§13), **the focused independent-review policy** (§14), **that request-volume values may not be invented and how a deferred count is resolved** (§§15–16), **that operational receipts are outside the accepted S5 and S6 identity graphs** (§17), **that nothing operational may contaminate a governed identity** (§18), **that no identity, secret, or restricted payload may appear in a receipt** (§19), and **that no Milestone 3 implementation authority is granted** (§20). It is **not** an architecture, boundary, acceptance, or closeout record and resolves no question belonging to another: **Decision 024 controls the M2 → M3 obligation transfer and the phase map itself**, Decision 026 the Milestones 0–2 closeout, Decision 021 the S6 architecture, Decision 022 item-46 applicability, and Decision 023 S6 acceptance and limitations O1–O4. Read §20 first if the question is whether any Milestone 3 implementation may now begin — **it may not**. Its plan is [`Milestones/milestone_03_master_plan.md`](../../Milestones/milestone_03_master_plan.md) and its documentation pack is [`Docs/m3/`](../m3/operator_runbook.md) |
| **Final closeout of Milestones 0, 1, and 2; final integrated acceptance; the completion tags; and the Milestone 3 master-planning handoff** | **Decision 026 — `ACCEPTED — OWNER APPROVED 2026-07-31`; binding.** The controlling record for **whether Milestone 0 is formally closed** (§6), **whether Milestone 1 is formally closed** (§7), **whether Milestone 2 is formally closed** — M2.1 (§8), M2.2 (§9), and M2.3 through accepted Stage S6 (§10) — **the closeout baseline** (§2), **the review chain closure rests on** (§3), **the final rereview outcome and the sixteen final classifications** (§§4–5), **the completion confirmations** (§11), **that the inherited limitations register stays active** (§12), **the formal outcome `MILESTONES_0_1_2_FORMALLY_ACCEPTED_AND_CLOSED`** (§14), **the authorization of the three annotated completion tags `m0-complete`, `m1-complete`, and `m2-complete`** (§15), and **that the next authorized action is `MILESTONE_3_MASTER_PLANNING`** (§18), with §§19–20 bounding what that planning may and may not do. It is **not** an architecture, acceptance, boundary, or correction record and resolves no question belonging to another: Decision 021 controls the S6 architecture, Decision 022 item-46 applicability, Decision 023 S6 acceptance and limitations O1–O4, **Decision 024 the Milestone 2 → Milestone 3 obligation transfer**, and **Decision 025 the integrated-audit documentation corrections**. Read §21 first if the question is whether anything in Milestone 3 may now begin — **it may not** |
| Cohort windows, maturity gates, bootstrap seed | Decision 003 (v0.2, live), cohort windows unchanged by Decision 010 |
| **Cohort date-source rule** (which date assigns `official_filing_temporal_cohort`) | **Decision 010** — official SEC filing date, not acceptance date |
| Primary outcome formula | Decision 002 — approved by project owner (2026-07-27 clarification, corrected 2026-07-27, corrected again 2026-07-27 fourth pass: `primary_universe_eligible` is not a SIC-alone flag — it requires an eligible, non-control candidate, sufficiently resolved required evidence, SIC outside 6000–6999, and no other Decision 002 exclusion; the M2.3 pilot's **eight** primary-universe-ineligible entities — four boundary controls plus four operating-financial-institutions quota entities — are engineering-only and never enter primary outcome construction) |
| SEC universe, canonical CIK identity | Decision 007 |
| Filing inventory, amendment handling | Decision 008 |
| Raw-data storage architecture | Decision 009 |
| EDGAR operating-calendar provenance | Decision 011 |
| Accession observation field resolution | Decision 012 |
| M2.3 pilot selection mechanics | Decision 013 |
| M2.3 pilot evidence levels and classification | Decision 014 |
| M2.3 pilot-use prohibition | Decision 015 |
| M2.3 Stage S3 schema and artifact architecture | Decision 016 |
| M2.3 Stage S4 quota-policy version, `excluded_pool_count`, boundary-control evidence interpretation | Decision 017 |
| M2.3 Stage S5 accession selection policy (roles, caps, floors, accession identity and hashing, `selected_order`, S4-draft disposition and S5 run identity, families and linked amendments, cross-cutting quota operationalization, node-limit/failure/retry semantics, S5 stage boundaries) | Decision 018 |
| M2.3 Stage S5 frozen-storage-to-pure-input mapping | **Decision 019** — `APPROVED — OWNER APPROVED 2026-07-28`; the controlling record for amendment-linkage evidence conversion (§5), multi-registrant evidence aggregation (§6), explicit pre-study support provenance (§7), and former-name identity-evidence conversion (§8), plus the snapshot-freeze obligations (§9) and the run-identity content those mappings contribute (§10) |
| M2.3 Stage S6 manifest construction, terminal result identity, and the publication boundary | **Decision 021 v0.5 — `ACCEPTED`, owner approved 2026-07-30; binding.** It is the controlling record for the pilot-manifest hash contract, `selection_result_sha256`, the pilot-manifest **document** schema, manifest identity and its immutability, manifest eligibility, and the proposed-only publication boundary, alongside Decision 013 §§7–8, Decision 016 §§5, 8, and milestone plan §10, which it operationalizes rather than replaces. Its §13.2.1 freezes the exhaustive 81-item milestone-plan §10 crosswalk and its counts; its §15.1 freezes the complete **eight-block** migration-`0013` SQL and its §15.3 the nine normative digests, byte and line counts, and concatenation rule; its §15.5 states the append-once and identity guarantee across every direct SQLite write path, and its §19.11 records the v0.4 finding on `pilot_selection_runs` row replacement, deletion, and identity mutation as **closed** by triggers 6, 7, and 8. **Stage S6 is implemented and accepted** (Decision 023): `release/pilot_manifest.py`, `sec/pilot_manifest_store.py`, and migration `0013` exist, and `selection_result_sha256` and `pilot_manifest_versions` now have their S6 writer. `pilot_projection_recovery_events` stays unwritten (§16), the only manifest state S6 can create is `proposed`, and no production catalog or real snapshot exists |
| M2.3 Stage S6 reserve-rank applicability (crosswalk item 46) | **Decision 022 — `ACCEPTED — OWNER APPROVED 2026-07-31`; binding, and the controlling record for whether item 46's reserve rank must be rendered for a selected target that has no compatible reserve package.** It rules that reserve rank is applicable **once per persisted reserve package** and is **structurally not applicable** where the target instead carries the persisted `REVIEW_PILOT_NO_COMPATIBLE_RESERVE` disposition, and that this non-applicability never makes a feasible S5 run manifest-ineligible. Decision 021 remains the controlling record for everything else about item 46 — its number, its `D` classification, its block 12 placement, and its `reserves_sha256` binding are unchanged — and **item 70 remains the total reserve-coverage requirement**. Read Decision 022 with Decision 021 §13.2.1 and §11.2 and Decision 020 §7.1; it changes no crosswalk row, count, preimage, identity, canonicalization rule, or migration byte |
| **The final integrated Milestones 1–2 audit result, the bounded documentation corrections, and the independent-verification handoff** | **Decision 025 — `ACCEPTED — OWNER APPROVED 2026-07-31`; binding.** The controlling record for **what the final integrated audit concluded** (§2), **the one bounded documentation defect and the navigation defect** (§§4–5), **what was corrected** (§6), **what the correction did not change** (§7), **the independence disclosure** (§8), and **the required sequence to closeout** (§9). It is not an architecture or acceptance record: Decision 021 controls the S6 architecture, 022 item-46 applicability, 023 S6 acceptance and limitations O1–O4, and 024 the Milestone 2/3 boundary. **Milestones 1 and 2 are not closed by this record** |
| **The deviation register** | **`Docs/preregistration.md` §25** — the canonical preregistration deviation register, currently holding **Deviation D001** (the Decision 010 cohort date-source rule). An accepted decision may explain or approve a deviation; §25 remains the register of record unless a later accepted decision explicitly replaces it. Signposted from `Docs/decision_index.md` and CLAUDE.md's reading order (Decision 025 §6) |
| **The Milestone 2 completion boundary, the former-S7–S10 obligation transfer, the Milestone 3 phase map, and the integrated-audit handoff** | **Decision 024 — `ACCEPTED — OWNER APPROVED 2026-07-31`; binding.** The controlling record for **where Milestone 2 implementation ends** (accepted S6), **what Milestone 2 finally consists of** (§3), **why it is not yet closed** (§4), **where the former S7–S10 obligations now live** (§5, the M3.1–M3.5 map and the §5.2 traceability table), **what Milestone 3 inherits** (§6), **which record answers which question** (§7), **that no implementation authority is granted** (§8), and **what happens next and in what order** (§9). Read §10 for the negative confirmations and §11 for the nonchange statement. It is **not** an architecture record and resolves no question belonging to another: Decision 021 controls the S6 architecture and the original S7–S10 scope definitions this record renames, Decision 022 controls item-46 applicability, and Decision 023 controls S6 acceptance, the path ratification, and limitations O1–O4 |
| **M2.3 Stage S6 acceptance, delivered-path ratification, accepted limitations, and checkpoint authorization** | **Decision 023 — `ACCEPTED — OWNER APPROVED 2026-07-31`; binding.** The controlling record for **whether Stage S6 is accepted** (it is — `M23_STAGE_S6_ACCEPTED_AND_COMPLETE`), for **which paths the delivered S6 change set legitimately contains** (the contract's seven plus three ratified forced-consequence test paths — §4), for the **four accepted nonblocking limitations O1–O4** (§7), and for the **checkpoint authorization** (§8: one commit, one push, the annotated tag `m2.3-s6-complete`, one tag push). It is **not** an architecture record: Decision 021 controls the S6 architecture and Decision 022 controls item-46 applicability, both unchanged. Read §6 for the nine invariance confirmations and §9 for the forward boundary — no S7, no live SEC operation, no Milestone 3, and Milestone 2 not yet closed |
| M2.3 Stage S5.4 reserve architecture and quota-contribution membership | **Decision 020 — `APPROVED — OWNER APPROVED 2026-07-30`; binding, and the controlling record for reserves at S5.4** alongside Decision 013 §6 and Decision 016 §7, which it operationalizes rather than replaces. The owner's nine rulings are in its §14, the migration-`0012` ruling in §8.2, and the test-scoping clarification in §8.3. The focused independent governance re-review of the exact `0012` DDL — table and all four triggers — recommended `ACCEPT_DECISION_020_FOR_OWNER_APPROVAL`, closing the 2026-07-29 lifecycle defect. The §8.2 SQL is frozen and was reproduced verbatim in migration `0012`, which is **created and accepted**. **Stage S5.4 is accepted and complete** (2026-07-30, `ACCEPT_M23_S5_4_FOR_CHECKPOINT`); §19 records that acceptance and §19.1 the five accepted methodological limitations. The record authorizes no further implementation |

## Open items this registry surfaces but does not resolve

- As of 2026-07-27: Decision 001 remains "Accepted working decision; final literature refresh still
  required" and Decision 004 remains "Proposed freeze pending project-owner approval" in their own
  files. This registry reports that status verbatim; it does not grant or withhold approval on
  their behalf. Decision 002 was approved and clarified on 2026-07-27 (see Index) and is no longer
  in this pending set. Anyone relying on Decision 004 (evaluation protocol) as *approved* should
  confirm current status directly, since this registry is a point-in-time index and the decision
  files are the source of truth.
- `decision_003_temporal_split.md` (unsuffixed, live) and `decision_003_temporal_split_v0_2.md`
  (archived) held byte-identical Decision 003 v0.2 content **before** the 2026-07-27
  governance-repair pass. Only the live file was then given the Decision 010 supersession banner,
  since it is the file named without a version suffix and is therefore treated as the live record.
  The archived `_v0_2` copy was left untouched and remains the unmodified historical snapshot of
  what the live file said before that banner was added.
