# Stage S6 — Pilot Manifest Construction, Terminal Result Identity, and the Publication Boundary

```
STAGE: M2.3 S6
STATUS: ACCEPTED_AND_COMPLETE
IMPLEMENTATION_AUTHORIZATION: NO
ACTIVE_BLOCKER: none — Stage S6 is implemented, independently rereviewed, independently accepted, and owner-accepted 2026-07-31 through Decision 023. Validation is complete and the checkpoint is authorized. This contract remains on record as Stage S6's scope statement and authorizes no new work
```

This contract states what Stage S6 is for and exactly what governs it, so a future session does not
have to re-read the full M2.3 plan and every referenced decision. **Its controlling record, Decision
021 v0.5, is ACCEPTED and binding as of 2026-07-30**, and **Stage S6 is now
`ACCEPTED_AND_COMPLETE`**.

**Acceptance, recorded 2026-07-31 (see "Acceptance" below).** The stage was implemented under
separately issued bounded authorizations, corrected once under the owner clarification
[Decision 022](../../Docs/Decisions/decision_022_m23_s6_reserve_rank_applicability.md), rereviewed
independently (`ACCEPT_M23_S6_IMPLEMENTATION_FOR_ACCEPTANCE_REVIEW`), and accepted on the final
independent acceptance review's recommendation
**`ACCEPT_M23_S6_FOR_OWNER_ACCEPTANCE_RECORDING`**. Implementation, validation, and independent
review are **complete**. **Like every completed contract in this directory, it now authorizes
nothing further** ([`README.md`](README.md)); reopening Stage S6 requires a new explicit owner
authorization and its own contract.

**Revised 2026-07-30 for Decision 021 v0.2, then v0.3, then v0.4, then v0.5.** The focused independent
governance review of v0.1 produced six bounded owner corrections, applied at v0.2. v0.3 applied one
further correction, to the structural fingerprint only — the tuple is **five columns**
(`region`, `state`, `observed_type`, `member_name`, `record_path`), not the three v0.2 froze.

**v0.5** applies one bounded owner ruling issued after the focused independent governance review of
v0.4. That review accepted the §10 crosswalk and the five-trigger manifest design, and proved by
direct probe that `pilot_selection_runs` was still open on three fronts the manifest table had just
been closed on — **row replacement, deletion, and identity mutation**. Migration `0013` therefore
grows from five triggers to **eight**: trigger 6 `pilot_selection_run_replacement_guard`, trigger 7
`pilot_selection_run_delete_guard`, and trigger 8 `pilot_selection_run_identity_guard`. **The five
v0.4 blocks are retained byte-for-byte and keep their per-block digests; trigger 2 is neither widened
nor renamed.** Decision 021 §15.5 now states the append-once and identity guarantee without
qualification, and §19.11 — the finding v0.4 could not resolve — is **closed**.

**v0.4** applied two bounded owner corrections issued after the focused independent governance
review of v0.3, which returned `REQUIRES_OWNER_CLARIFICATION`:

- **Correction A — the exhaustive milestone-plan §10 crosswalk.** The review found that nineteen §10
  items and four partially covered items were neither serialized nor classified, while the record
  claimed only two deliberate omissions. Decision 021 §13.2.1 now enumerates §10 **atomically** —
  **81 atomic items**, every compound bullet split — and classifies each into exactly one of four
  categories with a frozen count and **zero unclassified**. The §13.2 heading's "twelve blocks" over
  a thirteen-row table is corrected to **thirteen** in the same pass.
- **Correction B — migration `0013` grows from four triggers to five.** The review demonstrated that
  six-field identity immutability held on the `UPDATE` path only: `INSERT OR REPLACE` rewrote a
  manifest row wholesale past every guard, including over an `owner_approved` manifest. Trigger 5,
  `pilot_manifest_versions_replacement_guard`, closes all three uniqueness routes on the table.

Across all four revisions the **seven authorized implementation paths are unchanged**, and so are
the status, the implementation authorization, the three gating conditions, the 81-item §10 crosswalk
and every classification, the eleven-field `selector_policy_sha256` layer, every digest preimage, the
five-column fingerprint, the `manifest_id` derivation and its six-field immutability, the lifecycle,
the serialization location, the S4/S5 and S7–S10 boundaries, and the CLI deferral. **Neither the
exhaustive crosswalk nor the v0.5 triggers required a preimage change.** What changed at v0.4: the
document contract gained §13.2.1 and the region grew from four blocks to five. What changed at v0.5:
**the region grew from five blocks to eight**, so the v0.4 five-block region — its 7436-byte and
129-line counts and its concatenation digest `6bfb897c…` — is **withdrawn as a composition**, along
with the earlier v0.3 and v0.1 regions. Blocks 1–5 keep their exact bytes and their individual
digests, which are **not** withdrawn.

**Nothing is left unresolved.** The v0.4 open finding at Decision 021 §19.11 is **closed** by
triggers 6, 7, and 8, and §15.5 records the resulting guarantee: every new run begins unsealed, an
existing run cannot be replaced, a run cannot be deleted, the persisted run identity cannot change,
sealing happens only through the guarded update on an already-`feasible` run, a sealed digest cannot
change or clear, identical restatement stays idempotent, `selection_input_sha256` cannot change
before or after sealing, and `selection_result_sha256` is therefore append-once **and remains
recomputable from its persisted preimage** across every direct SQLite write path.

## Acceptance — recorded 2026-07-31

**Owner acceptance is recorded through
[Decision 023](../../Docs/Decisions/decision_023_m23_s6_acceptance_and_path_ratification.md)**
(`ACCEPTED — OWNER APPROVED 2026-07-31`), whose formal outcome is
`M23_STAGE_S6_ACCEPTED_AND_COMPLETE`. That record also:

- ratifies **three forced-consequence test paths** outside the seven this contract authorized —
  `tests/unit/test_storage_catalog.py`, `tests/unit/test_m23_entity_selection_store.py`, and
  `tests/unit/test_m23_accession_selection_store.py` — each an unavoidable consequence of authorized
  migration `0013` (see "Authorized implementation paths" below and Decision 023 §4);
- records four accepted nonblocking limitations, **O1**–**O4** (Decision 023 §7);
- authorizes exactly one commit, one push, the new annotated tag `m2.3-s6-complete`, and one tag
  push — and nothing else (Decision 023 §8).

**Decision 021 and Decision 022 are unchanged by acceptance** and remain controlling for the S6
architecture and for item-46 applicability respectively. No hash preimage, digest, crosswalk row,
classification total, migration byte, or S4/S5 behaviour changed at acceptance (Decision 023 §6).

## Gating — three conditions, all now satisfied

Stage S6 implementation required **all three**, in this order:

1. a **focused independent governance review of v0.5** of
   [Decision 021](../../Docs/Decisions/decision_021_m23_s6_manifest_construction.md) and of this
   contract, covering the exact **eight-block** §15.1 migration-`0013` SQL and its **nine** §15.3
   digests, the **§15.5 append-once and identity guarantee** and its nine clauses, the **exhaustive
   81-item milestone-plan §10 crosswalk in §13.2.1** and its frozen counts, every digest preimage in
   §§6–9 **including the eleven-field §8.4 selector-policy layer and the five-column §8.1 structural
   fingerprint**, the §10 circularity exclusions and the §10.1 commitment closure, the §9.2
   six-field identity-immutability ruling, and the §13 pilot-manifest document contract —
   **SATISFIED, 2026-07-30**. The review reached its own conclusion, inherited no earlier
   recommendation, reproduced all nine §15.3 digests from the record bytes, applied the extracted
   SQL to a scratch `0001`–`0012` catalog, ran 318 adversarial assertions across all four
   `recursive_triggers` × `foreign_keys` combinations, and returned
   `ACCEPT_DECISION_021_V05_FOR_OWNER_APPROVAL` with **no governance blockers and no owner
   clarifications required**;
2. **owner acceptance of Decision 021 v0.5**, recorded as approved in
   [`Docs/Decisions/decision_registry.md`](../../Docs/Decisions/decision_registry.md) —
   **SATISFIED, owner approved 2026-07-30**; the record now reads `ACCEPTED` and is binding;
3. a **separately issued bounded S6 implementation authorization** that does not widen the authorized
   paths below — **SATISFIED.** Bounded S6 implementation authorizations were issued and exercised,
   and a further bounded correction was authorized by Decision 022 §7 after the reserve-rank
   applicability clarification. Implementation is complete.

**A cleared blocker was never authorization to implement**, and this contract read
`READY_FOR_IMPLEMENTATION` with `IMPLEMENTATION_AUTHORIZATION: YES` for exactly that reason — it
recorded that nothing external blocked the stage and did not start the work
([`README.md`](README.md)). That distinction is preserved here as the historical record of how the
stage was gated. **It now reads `ACCEPTED_AND_COMPLETE` with `IMPLEMENTATION_AUTHORIZATION: NO`**,
which means the stage shipped and was accepted, and that this contract authorizes no new work.

## Baseline prerequisites

Reverify live with `scripts/context_snapshot.sh` (`make context`) before trusting anything here;
these are point-in-time references, not live state.

- Branch `main`; accepted checkpoint tag `m2.3-s5.4-complete`, supplementing the immutable
  `m2.3-s5-complete`.
- Migrations end at `0012_m23_selection_entity_reasons.sql`.
- Stages S4, S5.1, S5.2, S5.3, and S5.4 are accepted, committed, and closed. Decision 020 is
  `APPROVED — OWNER APPROVED 2026-07-30`.
- `pilot_selection_runs.selection_result_sha256` is `NULL`; `pilot_manifest_versions` and
  `pilot_projection_recovery_events` have no writer; no candidate-snapshot builder exists and no
  production catalog exists.

## Objective

Implement the deterministic pilot-manifest and terminal-result **machinery**, and **define and
fixture-test the complete pilot-manifest document schema**: the eight component hashes plus the root,
`selection_result_sha256`, the canonical pilot-manifest document and its JSON rendering, persistence
of a **`proposed`** manifest record, reconstruction and verification of all three, and the
**eight** migration-`0013` lifecycle, identity, replacement, and deletion guards. Governed by
[Decision 013](../../Docs/Decisions/decision_013_pilot_selection_mechanics.md) §§7–8,
[Decision 016](../../Docs/Decisions/decision_016_m23_schema_and_artifact_architecture.md) §§1, 5, 8,
[Decision 018](../../Docs/Decisions/decision_018_m23_s5_accession_selection_policy.md) §22,
[Decision 020](../../Docs/Decisions/decision_020_m23_s5_4_reserve_architecture.md) §§9, 11, 14.4,
[`milestone_2_3_pilot_selection_plan.md`](../milestone_2_3_pilot_selection_plan.md) §10 and §16,
and — controlling for this stage —
[Decision 021](../../Docs/Decisions/decision_021_m23_s6_manifest_construction.md) v0.5.

## Governing authorities

- **Decision 022 — `ACCEPTED`, owner approved 2026-07-31; binding** — the controlling record for
  **one point only**: whether Decision 021 §13.2.1 crosswalk item 46's reserve rank must be rendered
  for a selected target that has no compatible reserve package. It rules that reserve rank is
  applicable **once per persisted reserve package** and is **structurally not applicable** for a
  target carrying the persisted `REVIEW_PILOT_NO_COMPATIBLE_RESERVE` disposition instead; that such
  non-applicability never makes a feasible S5 run manifest-ineligible; that **item 70 remains the
  total per-target reserve-coverage requirement** — exactly one rank-1 package or exactly one
  no-compatible-reserve disposition per selected target; that a zero-package run is eligible only
  with complete, non-duplicated, non-conflicting disposition coverage; that mixed runs remain valid;
  and that **no synthetic package, `reserve_rank = 0`, `null`, `"N/A"`, placeholder, or invented rank
  may be created or serialized**. Applicability is derived from persisted `pilot_reserves` rows and
  persisted reserve-scope `pilot_selection_entity_reasons` rows only — never from a caller flag, a
  fixture parameter, or a runtime assumption. **It supersedes and amends nothing**, adds no
  authorized path, and changes no crosswalk row, item number, classification total, digest binding,
  hash preimage, manifest identity, canonicalization rule, or migration SQL byte. Decision 021
  controls item 46 in every other respect.
- **Decision 021 v0.5 — `ACCEPTED`, owner approved 2026-07-30; binding** — this stage's controlling
  architecture record: scope (§4), hashing
  infrastructure (§5), the `selection_result_sha256` preimage (§6), the four terminal component
  boundaries (§7), the four remaining component preimages including the **extended eleven-field
  selector-policy layer and the five-column structural-fingerprint partition rule** (§8), the root (§9), the
  `manifest_id` derivation (§9.1) and **six-field identity immutability** (§9.2), the circularity
  exclusions (§10) and **commitment closure** (§10.1), lifecycle/eligibility/transactions (§11),
  reconstruction and replay (§12), **the complete pilot-manifest document contract** (§13), the
  S4/S5 boundary (§14), the schema ruling and the complete frozen **eight-block** migration-`0013`
  SQL and digests (§15), the no-new-surfaces and **CLI-narrowing** ruling (§16), the **S7–S10**
  boundary (§17), accepted limitations (§19), test obligations (§20), stop conditions (§21), and the
  checkpoint boundary (§22). **Status `ACCEPTED`; binding.**
- **Decision 013 §7** — canonical JSON rules, the nine hash layers, and the reuse of
  `release/hashing.py` rather than a parallel scheme. **§8** — M2.3 completion is owner approval of
  the exact final manifest hash, which is Stage S10, not this stage.
- **Decision 016 §1** — content-derived 64-hex IDs. **§5** — the manifest lifecycle. **§8** — hash
  boundaries, the excluded fields, the source-content hash definition, and the `releases/` tree.
- **Decision 018 §22** — the S6 stage boundary. **§25** — a schema gap is an owner-level conflict,
  never a migration a session writes on its own. **§§6, 27** — the S4 draft's permanent exclusion.
- **Decision 020 §9** — reserves are subordinate content under the accepted S5 run ID, and
  `pilot_manifest_versions.reserves_sha256` is named as **the S6 consumer**. **§11** — what S5.4 did
  not change. **§14.4** — the `selection_result_sha256` question reserved for this stage.
- **Milestone plan §10** — "Required pilot-manifest contents", the specification Decision 021 §13
  operationalizes. **§16** — the staged decomposition, whose M2.3A-6 CLI element Decision 021 §16
  deliberately defers to Stage S9. Neither is superseded; both are cited rather than paraphrased.
- **Migrations `0009`–`0012`** — the persisted contract, unchanged by this stage.

## In scope

- The eight component digests and `root_manifest_sha256`, at exactly the preimages Decision 021
  §§6–9 freeze — including the **eleven-field** `selector_policy_sha256` (§8.4) and the
  **five-column structural-fingerprint partition rule** (§8.1).
- `selection_result_sha256` computation and its append-once sealing.
- **The complete pilot-manifest document schema** — all thirteen Decision 021 §13.2 blocks, **the
  exhaustive 81-item milestone-plan §10 crosswalk in §13.2.1 and its frozen counts**, the
  §13.3 completeness rule, and canonical JSON under `DataTree.releases / "pilot"` with a
  content-derived filename — **defined and fixture-tested here**. Stage S9 supplies the exact
  real-data instance.
- One `proposed` `pilot_manifest_versions` row, written in a single transaction with its serialized
  document, with all six identity fields fixed at `INSERT` and never updated (§9.2).
- Fail-closed eligibility checks (Decision 021 §11.2) and a public verification entry point that
  re-derives the digests, the root, `manifest_id`, and the document.
- Migration `0013`, reproducing the Decision 021 §15.1 **eight-block** SQL byte-for-byte and its
  **nine** §15.3 digests, and establishing the §15.5 append-once and identity guarantee.

## Deferred

- **Owner approval, publication, and the exact real-data manifest instance** — Stage S10 / S9, not
  this stage.
- **Live-metadata safety and allowlist (Gate F)** — **Stage S7**.
- **Live SEC metadata execution and the candidate-snapshot builder** — Stage S8 / S9.
- **CLI output**, and with it milestone plan §10's "command invocation" field — **Stage S9**
  (Decision 021 §16, §13.2). `cli.py` is untouched at S6.
- **Projection-recovery writing** (`pilot_projection_recovery_events`) — later operational stage.
- **The M2.5 replacement event**, forecasts, and outcomes.
- **The full Milestone 2 integrated review** — only after **Stage S10** and the exact owner-approved
  manifest (Decision 021 §17); **not after S6**, and not after S7, S8, or S9.

## Authorized implementation paths

**As authorized, these seven paths were the complete set, and none of the v0.2, v0.3, v0.4, or v0.5
corrections added an eighth.** Each correction lands inside a path already listed: the document
schema and the five-column fingerprint reduction are pure logic in `release/pilot_manifest.py`; the
row loading the fingerprint's structural rows and the six explicit
arguments feed into `sec/pilot_manifest_store.py`; the fourth trigger is more SQL in the same
migration file; and the widened obligations are more assertions in the four listed test modules. No
prompt, decision record, or status file widened this table. Every other production, test, migration,
decision, and contract path in the repository was prohibited unless later owner-authorized.

**That seven-path authorization is stated above exactly as it was issued, and is not rewritten.**
What the stage actually delivered was **ten** implementation and test paths: these seven, plus the
three test modules §"Delivered path set" below records, which
[Decision 023](../../Docs/Decisions/decision_023_m23_s6_acceptance_and_path_ratification.md) §4
ratifies retroactively.

| Path | Kind |
|---|---|
| `src/disclosure_drift/release/pilot_manifest.py` | new pure module — component digests, the §8.1 structural-fingerprint reduction, `selection_result_sha256`, the root, `manifest_id`, the §13.2 document schema and its canonical JSON rendering |
| `src/disclosure_drift/sec/pilot_manifest_store.py` | new persistence adapter — row loading, eligibility checks, sealing, one `proposed` manifest row, the serialized document, verification; takes the six Decision 021 §8.4 explicit arguments |
| `src/disclosure_drift/storage/migrations/0013_m23_manifest_lifecycle_guards.sql` | **new migration — DDL-only**, **eight** new triggers, reproducing the Decision 021 §15.1 eight-block SQL byte-for-byte and its nine §15.3 digests; creates no table, column, or index; edits no existing migration |
| `tests/unit/test_m23_pilot_manifest.py` | new primary test module — the pure hashing, document-schema, completeness, and serialization contract |
| `tests/unit/test_m23_pilot_manifest_store.py` | new test module — eligibility, sealing, persistence, atomicity, verification, explicit-argument discipline |
| `tests/unit/test_m23_pilot_schema.py` | bounded edit — the **eight** new triggers adversarially, **including the INSERT OR REPLACE routes on both tables, the DELETE refusals in every run state, the three selection-run identity fields, and the byte-preservation assertions**, and the `_insert_manifest` fixtures a sealed feasible run now requires (Decision 021 §20 states the three fixture changes and the two legitimate routes) |
| `tests/unit/test_migration_provenance.py` | bounded edit — extends contiguous-chain, ordering, and byte-immutability coverage to `0013` |

## Delivered path set — the seven above plus three ratified test paths

**Migration `0013` forced three further test edits that the seven-path table did not anticipate.**
Decision 021 §20 foresaw this class of consequence but named only `test_m23_pilot_schema.py`; the
same mechanism reached three more modules. The final independent acceptance review found them,
established that each was unavoidable, and referred the authorization gap to the owner rather than
resolving it. **Decision 023 §4 ratifies all three retroactively.**

| Ratified path | Why migration `0013` forced it |
|---|---|
| `tests/unit/test_storage_catalog.py` | asserts the canonical migration chain by exact version **and** name, so it fails until `(13, "m23_manifest_lifecycle_guards")` is present — no discretion |
| `tests/unit/test_m23_entity_selection_store.py` | its accepted S4 corruption fixture built its precondition with a plain `UPDATE` on `selection_input_sha256`, which trigger 8 now refuses |
| `tests/unit/test_m23_accession_selection_store.py` | same cause at four call sites; additionally its `_corrupt_sealed_row` helper dropped every `pilot_%_{insert,update,delete}_guard` by wildcard, which would have swallowed the new `pilot_selection_run_delete_guard` |

Verified at acceptance: **no production path changed, no S4 or S5 methodology changed, and no
assertion was removed, weakened, relaxed, skipped, or xfailed.** The rewritten corruption fixtures
are **narrower and more fail-closed** than the code they replaced — an allowlist restricting
corruption to catalogs the suite itself created, exactly one trigger removed per statement instead of
a wildcard family, that trigger restored from its own captured `sqlite_master` definition in a
`finally` block with reinstallation asserted, and foreign keys left enabled except where the
historical corruption being modelled is itself a broken reference.

**This is a ratification of three named paths, not a general widening** (Decision 023 §4.2). A future
session needing a path outside its contract still stops and reports under Decision 021 §21.

## Prohibited paths

- `src/disclosure_drift/storage/migrations/**` — **no migration other than
  `0013_m23_manifest_lifecycle_guards.sql`**, and none may edit, replace, or reinterpret `0009`–`0012`.
- `src/disclosure_drift/sec/accession_selector.py`, `accession_selection_store.py`,
  `reserve_selector.py` — accepted S5; regression only, never edited. The S5.2 reconstruction path is
  **not** extended to cover the seal; Decision 021 §12 puts that obligation on the new S6 module.
- `src/disclosure_drift/sec/entity_selector.py`, `entity_selection_store.py` — accepted S4.
- `src/disclosure_drift/release/hashing.py`, `release/manifest.py` — reused, never modified. The
  pilot manifest does not reuse `ReleaseManifest`, `build_manifest`, or `RELEASE_SCHEMA_VERSION`.
- `src/disclosure_drift/paths.py` — `DataTree.releases / "pilot"` is composed by the store; no new
  subtree entry and no new accessor.
- `src/disclosure_drift/cohorts.py`, `pilot_policy.py`, `reasons.py` — no frozen research definition,
  no policy constant, no reason code. Both constants this stage needs already exist.
- `src/disclosure_drift/cli.py` — no CLI behavior at this stage. Decision 021 §16 deliberately
  narrows milestone plan §16's M2.3A-6 ("Manifest serialization, hashing, and CLI output") by
  deferring the CLI element to Stage S9; the plan's stage table is **not** an authorization to add
  one here.
- `Docs/**`, `Literature/**`, `Milestones/**` — read-only during implementation (CLAUDE.md rule 14).
- Anything touching the S4 entity-only draft, which stays `running`, non-publishable, and excluded
  from every manifest input.

## Required APIs (provisional until Decision 021 is approved)

- `release/pilot_manifest.py`: the **§13.2 document schema covering all thirteen blocks and every
  one of the 81 atomic §10 items the §13.2.1 crosswalk classifies `D` or `T`**, with each `X` item
  provably absent from the substantive body and item 80 (command invocation) deferred to S9; pure
  functions producing each digest at its frozen preimage; the
  §8.1 structural-fingerprint reduction (partition by `parser_run_id`, normalize under the existing
  canonical rules, reduce each run to the distinct set of the frozen five-column tuple
  `("region", "state", "observed_type", "member_name", "record_path")`, require every run's set to be
  exactly equal, raise `GateFailureError` on any disagreement, hash the common set exactly once with
  `hash_table`, and exclude `parser_run_id` from the digest — **no three-column fallback and no
  second fingerprint methodology**); `manifest_id` at §9.1; a container for the eight components plus the
  root; the §13.2 document schema; and a canonical-JSON renderer. **No SQLite access, no clock, no
  filesystem, no environment, no Git, no `sys.version`.**
- `sec/pilot_manifest_store.py`: one entry point that loads the persisted rows, verifies
  eligibility, seals `selection_result_sha256` in its own prior transaction, then builds and
  persists a `proposed` manifest with its document in one transaction — all six identity fields
  fixed at `INSERT`, none ever updated; and one public verification entry point that re-derives the
  digests, the root, `manifest_id`, and the document, and fails closed. **Six required explicit
  arguments** (Decision 021 §8.4): `dependency_lock_sha256`, `code_commit_identifier`,
  `runtime_python_version`, `configuration_sha256`, `decision_authority_sha256`, and
  `source_plan_sha256`. None may be inferred from Git, the environment, the interpreter, the
  configuration, the decision directory, or the working tree.
  **Plain `INSERT` and plain `UPDATE` only (v0.4; extended at v0.5).** The store may never issue
  `INSERT OR REPLACE`, `REPLACE INTO`, `INSERT OR IGNORE`, or `DELETE` against
  `pilot_manifest_versions` or `pilot_selection_runs`, and may never name **`selection_run_id`,
  `snapshot_id`, or `selection_input_sha256`** in an `UPDATE … SET` list — the only run column it
  ever updates is `selection_result_sha256` (Decision 021 §16). **Idempotent replay reads,
  reconstructs, compares, and returns the existing manifest without writing** (Decision 021 §11.3,
  §12); a mismatch is a `GateFailureError`, never a rewrite.
  **`selection_input_schema_version` is a code constant, not a persisted run column.** It is not one
  of `pilot_selection_runs`' 18 columns (Decision 021 §3.6); it is supplied as the accepted
  `ACCESSION_SELECTION_INPUT_SCHEMA_VERSION` in `sec/accession_selection_store.py`, is immutable by
  absence, and is bound by Decision 021 §6.1 and §8.4. Trigger 8 therefore names three columns.

## Required tests

The complete list is Decision 021 §20 and is not restated here (contracts link rather than
duplicate — see [`README.md`](README.md)). The minimum categories: determinism and permutation
invariance; per-component sensitivity; exclusion proofs; column-order normativity; circularity;
adversarial eligibility across all seven conditions; nonblocking dispositions; proposed-only;
**six-field manifest-identity immutability**; **append-once on every write path, including a
pre-sealed `INSERT` and a seal riding along with the terminal transition**; **document completeness
under §13.3, mapping every serialized leaf to the digest that binds it**; **the exhaustive §10
crosswalk asserted item by item — all 81 rows with their classifications, the six frozen counts with
`unclassified == 0`, every `D` and `T` item present in the document at §10 granularity and compared
against its recorded reconstruction source, every `T` item's covering digest proven sensitive to it,
every `X` item provably absent from the substantive body, and item 80 the only S9 deferral**;
**no unbound and no undocumented substantive field — a serialized value with no committing digest,
or a document field absent from the crosswalk, fails the suite**; **`manifest_state` refused for any
state other than `proposed`**; **the replacement path adversarially — each of the three uniqueness
routes (`manifest_id`; `UNIQUE (selection_run_id, snapshot_id, ordinal_version)`; the partial index
`uq_pilot_manifest_single_active_approval` over an `owner_approved` manifest) refused under all four
combinations of `recursive_triggers` and `foreign_keys`, so no pragma is required for correctness;
the pre-existing manifest row byte-identical across every column after every refused attempt; a
genuinely new `proposed` manifest and an ordinal-2 successor still accepted; an identical-content
`INSERT OR REPLACE` replay and an `INSERT OR IGNORE` duplicate both refused; and no S6 module
containing `INSERT OR REPLACE`, `REPLACE INTO`, `INSERT OR IGNORE`, or `DELETE` against either
table, and no S6 module naming `selection_run_id`, `snapshot_id`, or `selection_input_sha256` in an
`UPDATE … SET` list**; **the selection-run guarantee adversarially — replacement refused as a plain
duplicate `INSERT`, an `INSERT OR IGNORE`, and an `INSERT OR REPLACE` carrying an identical digest, a
changed digest, or none, and one changing `snapshot_id` or `selection_input_sha256`, over a run in
every run state; `DELETE` refused in every run state with no child row present; each of the three
persisted identity fields refused independently with an identical three-field restatement accepted;
all of it under every combination of `recursive_triggers` and `foreign_keys` so no pragma is required
for correctness**; **original-row byte preservation across all 18 columns after every refused
`UPDATE`, `REPLACE`, `INSERT` conflict, and `DELETE`, on both tables**; **the nine Decision 021 §15.5
clauses proven as a set, including that a sealed `selection_result_sha256` still recomputes from its
persisted §6.1 preimage after every refused write — append-once and recomputability across every
direct SQLite write path**; **idempotent replay proven to read, reconstruct, compare, and return
without writing**; **all thirteen §13.2
document blocks**; **the five-column structural-fingerprint rule, with all six required proofs — an
identical reparse is a digest no-op, a duplicate identical row is a digest no-op, parser-run order
and row order are irrelevant, a disagreement between parser-run sets fails closed, each of the five
tuple fields changes the fingerprint independently, and no three-column fallback or second
fingerprint methodology exists**; **explicit-argument discipline and the
no-leakage-surface assertion**; atomicity under injected fault; serialization; reconstruction and
replay; the S4 draft refused at three layers; migration `0013` adversarially — the **223** §15.4
assertions, byte-faithfulness to the **eight-block** §15.1 SQL, and reproduction of the **nine**
§15.3 digests with their byte and line counts, the withdrawn v0.4 7436/129 and v0.3 4990/88 regions
provably absent; and regression without editing across the S4, S5.1, S5.2, S5.4, reserve,
reason, catalog, release, and migration-provenance suites.

## Validation gates

Progressive, per [`Docs/change_impact_map.md`](../../Docs/change_impact_map.md): the new pure
module's own tests; the store's tests; migration `0013` schema and provenance tests; `make
lint-changed`; `make format-check`; `make typecheck`; then `make check` **exactly once** after all
targeted checks pass. Because this stage adds migration `0013` and writes migration-`0009` tables,
`tests/unit/test_migration_provenance.py`, `tests/unit/test_m23_pilot_schema.py`, and `make
sqlite-check` (floor 3.37) all apply.

## Independent review stages

0. **Focused governance review of Decision 021 v0.1** — **COMPLETE**, 2026-07-30. Recommendation
   `REQUIRES_OWNER_CLARIFICATION`; it produced the six bounded owner corrections that v0.2 applied.
   **v0.2 itself was never independently reviewed and never approved.**
1. **Focused governance re-review of Decision 021 v0.3** — **COMPLETE**, 2026-07-30. Recommendation
   `REQUIRES_OWNER_CLARIFICATION`. It confirmed the five-column fingerprint, the eleven-field §8.4
   layer, the acyclic digest graph, and the then-frozen four-block SQL and its digests, and found
   three defects: an incomplete milestone-plan §10 crosswalk, identity immutability holding on the
   `UPDATE` path only, and the "twelve blocks" heading. **v0.3 was not accepted for approval.** Its
   findings produced owner corrections A and B, applied at v0.4.
2. **Focused governance review of Decision 021 v0.4** — **COMPLETE**, 2026-07-30. Recommendation
   `REQUIRES_OWNER_CLARIFICATION`. It accepted the 81-item §10 crosswalk and the five-trigger
   manifest replacement design, and proved by direct probe that `pilot_selection_runs` was still
   open to row replacement, deletion, and identity mutation. **v0.4 was not accepted for approval.**
   Its findings produced the v0.5 ruling — triggers 6, 7, and 8.
3. **Focused governance review of Decision 021 v0.5** — **COMPLETE**, 2026-07-30. Recommendation
   `ACCEPT_DECISION_021_V05_FOR_OWNER_APPROVAL`, with no governance blockers and no owner
   clarifications required; it produced one editorial correction to Decision 021 §13.2.1's
   explanatory arithmetic (**74 original bullets producing 81 atomic requirements**) and two
   non-blocking observations. **Owner approval followed the same day.** It covered the exact
   **eight-block** §15.1 SQL and its **nine** §15.3 digests, the **§15.5 append-once and identity
   guarantee**, the **exhaustive 81-item §10 crosswalk in §13.2.1** and its frozen counts, every
   preimage including the eleven-field §8.4 and the five-column §8.1 fingerprint, the §10.1
   commitment closure, the §9.2 identity-immutability ruling, and the §13 document contract. **It
   must reach its own conclusion and may not inherit the v0.1, v0.3, or v0.4 recommendation, or any
   view formed of v0.2.**
4. **S6 implementation review** — independent and adversarial, against this contract. **COMPLETE.**
   It found fifteen tracked items, all resolved under bounded corrections, and one conflict it
   correctly declined to resolve — crosswalk item 46 against a lawful zero-package run — which it
   referred under Decision 021 §§21 and 13.3 and which
   [Decision 022](../../Docs/Decisions/decision_022_m23_s6_reserve_rank_applicability.md) settled.
   The fresh independent rereview of the corrected tree returned
   `ACCEPT_M23_S6_IMPLEMENTATION_FOR_ACCEPTANCE_REVIEW`.
5. **S6 acceptance review** — independent, with the owner-facing recommendation. **COMPLETE**,
   recommendation **`ACCEPT_M23_S6_FOR_OWNER_ACCEPTANCE_RECORDING`**, with no methodological
   findings, no implementation defects, no test defects, no outstanding owner clarifications, and no
   acceptance blockers. It reproduced the decisive evidence independently — migration `0013`
   byte-for-byte against Decision 021 §15.1, all nine §15.3 digests, the frozen crosswalk counts, and
   nine acceptance-critical end-to-end guarantees — and raised the one governance item Decision 023
   §4 resolves.

## Validation

**Complete.** Focused S6 set 891 passed / 1 skipped; full suite 2324 passed / 2 skipped; parallel
suite 2324 passed / 2 skipped; alternate-temp-root suite 2324 passed / 2 skipped; `ruff check` and
`ruff format --check` clean; `mypy src` clean; `make sqlite-check` on SQLite 3.53.3 (floor 3.37);
`make secrets` 0 findings; `make hygiene` 0 findings; `make context` matching the expected baseline.

## Commit boundary

**One commit at S6 acceptance**, and only after both S6 acceptance is recorded and the project owner
explicitly instructs it. The checkpoint is the **new annotated tag `m2.3-s6-complete`**, which
**supplements** the immutable `m2.3-s5-complete` and `m2.3-s5.4-complete` (Decision 021 §22).
CLAUDE.md rule 13 applies independently.

**That boundary is reached.** Acceptance is recorded in Decision 023, which authorizes exactly one
commit, one push, the annotated tag `m2.3-s6-complete`, and one tag push (Decision 023 §8). **No
further work is authorized under this contract.** The next phase is a separate governance-only
Milestone 2 / Milestone 3 boundary session; the full Milestone 2 integrated review follows that
session, and Milestone 2 is not closed by S6 acceptance.

## Stop conditions

Decision 021 §21 is the complete list and binds any implementation session. In summary: stop and
report, without writing code, if Decision 021 v0.5 is not `ACCEPTED` in the registry; if no separately
issued bounded implementation authorization exists; if migration `0013` would differ in any way from
the frozen **eight-block** §15.1 SQL or fail to reproduce the nine §15.3 digests, byte count, or line
count; if any DDL beyond `0013` appears necessary; if a path outside "Authorized implementation
paths" is genuinely required; if a frozen preimage appears wrong, incomplete, or unimplementable, or
a schema column exists that no frozen tuple classifies; **if a §13.2 document block cannot be
populated from bound values, or a serialized field would have to be carried that no digest commits
(§13.3); if two parser runs over one cited observation disagree on their distinct
`(region, state, observed_type, member_name, record_path)` sets (§8.1), which is never resolved by
narrowing the tuple back toward the withdrawn v0.2 three-column form; if any of the six §8.4
explicit arguments is unavailable**; **if a §13.2.1 crosswalk item cannot be placed as classified,
since a session may never reclassify an item, add a category, or change a count**; **if any S6
module would need `INSERT OR REPLACE`, `REPLACE INTO`, `INSERT OR IGNORE`, or `DELETE` against
`pilot_manifest_versions` or `pilot_selection_runs`, or would need to name `selection_run_id`,
`snapshot_id`, or `selection_input_sha256` in an `UPDATE … SET` list**; **if any of the nine
Decision 021 §15.5 clauses cannot be reproduced against the migration as written**; **if a ninth
trigger or any further DDL appears necessary — migration `0013` is exactly eight triggers, and
growing it again is an owner-level act**; if producing a
manifest would require reading the S4 draft, running a second selection, applying a reserve
substitution, or relaxing an accepted S5 output; if any instruction asks to advance a manifest past
`proposed`, **mutate one of the six immutable identity columns**, approve, publish, **add a CLI
surface**, begin live metadata work or Stage **S7**/S8/S9/S10, or commit before acceptance; or if
build, runtime, configuration, authority, or plan identity would have to be inferred from Git, the
environment, the interpreter, or the working tree.
