# Milestone 3.2 — Controlled Metadata-Only SEC Acquisition (M3.2A / M3.2B) and Gate H

```text
STATUS: DRAFT — PENDING OWNER REVIEW AND ACCEPTANCE
IMPLEMENTATION_AUTHORIZATION: NO
NETWORK_AUTHORIZATION: NONE — this draft authorizes zero network access; the future
  per-window authorization it DESCRIBES is CONTROLLED AND EXPLICITLY AUTHORIZED for the one
  named command `m3 acquire`, and can only be granted by the separate owner instruments in §8
M3_2A_HARD_REQUEST_CEILING (owner-approved 2026-08-03, not usable under this draft): 801
```

**This is a DRAFT for ChatGPT owner review.** It was produced at Decision 029 §12 step 17 under
the owner's explicit step-17 authorization of 2026-08-03, which authorized **drafting only**. This
document is **not** accepted, **not** approved, **not** ready for implementation, **not**
operational, and **not** executable. It authorizes **no** implementation, **no** network or
CompanyFacts enablement, **no** contact with the SEC, **no** controlled acquisition, **no**
creation or population of the operational catalog, and **no** operational use of the approved
request ceiling. **No executable work begins until a later owner acceptance of this contract and a
separately issued explicit implementation authorization** (Decision 024 §8; master plan §8, §16).

## 1. Status and authority

- **Status:** `DRAFT — PENDING OWNER REVIEW AND ACCEPTANCE`. Acceptance requires: independent
  contract review by a session that authored none of this draft (master plan §13 review policy, as
  the owner directs), then explicit owner acceptance recorded in this header and the governance
  ledger. Only after acceptance can the separate Decision 024 §8 implementation authorization be
  considered.
- **Authority basis for this draft:** Decision 029 §12 step 17 (the frozen sequence's final step);
  the owner's step-17 authorization of 2026-08-03; master plan §8 ("every phase needs its own
  bounded contract") and §16 (mandatory contents); Decision 024 §§5.1, 5.2 (S8 row), 6, 8 (the
  obligations transferred intact from former M2.3 S8 and the five entry conditions this phase must
  satisfy per authorization).
- **Hierarchy:** this contract implements accepted records and overrides none of them. Where this
  draft and an accepted decision, migration, or policy module disagree, the accepted record
  controls and this draft must be corrected.

## 2. Baseline prerequisites

To be **re-verified live** (`make context` plus a read-only remote check) at acceptance and again
at every later gate; recorded here as the state at drafting:

- **Accepted M3.1 checkpoint:** commit `4cd2c7299ae30ca499108bd7f0a17a0adaf215f4`
  ("Record M3.1 acceptance"), annotated tag `m3.1-complete`
  (tag object `638a02b780d912ff7b37a2f523277b9d451a015a`, peeled to that commit), verified locally
  and remotely; `HEAD == origin/main` at drafting.
- **Frozen accepted implementation:** `970e050deb06910adcde8588101564beb7d19c74`
  (tree `d0c3c94cbf9128eaf0fdb1ef58179d9977d718d3`); implementation and test bytes byte-identical
  from the frozen SHA through the checkpoint commit.
- **Governance state:** Decisions 028–031 accepted; `M3_1_ACCEPTED_AND_COMPLETE` (Decision 031);
  first durable §17 review `M3_1_SECTION_17_REVIEW: PASS` (sanitized SHA-256 `9c40a829…`);
  independent step-14 acceptance review `M3_1_INDEPENDENT_ACCEPTANCE_REVIEW: PASS`
  (artifact SHA-256 `caf9f26e6a2690a05a9d6a238d5572533b858789638b35a24da06c64a4c5ae4e`, commit
  `24fba32413bb6c5dade60a64182e42510afe6f88`); M3-L11 and M3-L12 `CLOSED` (2026-08-03); D023-O1
  latent (§17 stop condition 15 here).
- **Migration chain:** contiguous through `0013`; **M3.2A adds no migration** (§11). Any M3.2B or
  catalog-related schema need would require its own decision and contract amendment — none is
  implied.
- **Readiness evidence (Gate F, M3.1B):** signed Gate F checklist SHA-256
  `34fc0567dd31b75b83d8bb12f31e172c04074bd1a0a3b1487b0461d170339fbc` (result `PASS`);
  readiness-token record SHA-256
  `b06ae373a184ee73c84b78a52b4761432403600a47038e972ecf1b894b0c9c8e`
  (`M3_1_GATE_F_READY_FOR_CONTROLLED_METADATA_ACQUISITION` — **readiness only; never
  live-operation authorization**, §15 item 16).

## 3. Objective (purpose)

Implement, validate, and — only under the §8 gate ladder — execute the controlled metadata-only
SEC acquisition the master plan M3.2 §§1–36 defines: acquire, under a per-window owner-approved
budget and hard ceiling, exactly the already-approved SEC **metadata** needed to later build a
real candidate snapshot (M3.3), deriving M3.2B's dependent requests from frozen M3.2A evidence
rather than estimating them, and prove at Gate H that both windows stayed inside every boundary.
This is the obligation formerly called M2.3 S8, transferred intact by Decision 024 §5.1.

## 4. Governing authorities

Cited, never restated: Decision 024 §§5.1, 5.2 (S8 row), 6, 8; Decision 027 v0.2 (as corrected by
Decision 028) — master plan global §§5–16 and phase M3.2 §§1–36 in full; Decision 007 (approved
sources, canonical CIK); Decision 008 (inventory, amendments); Decision 009 (raw-data governance;
§10 hashing; append-only); Decision 010 (temporal availability; official filing date as cohort
source); Decision 011 (operating-calendar provenance); Decision 012 (observation resolution);
Decision 013 §1 (as-of `2026-06-30`; closed 2026 Q2; `include_open_quarter = false`); Decision 028
(planner v2, receipt v2, ceiling equality, budget arithmetic, recovery ownership, M3-L11 layers);
Decision 029 (per-route `A_reachable` witness; ceiling formula `Σ U(route) × A_reachable(route)`;
reason semantics; §12 sequence); Decision 030 (Rulings C–E); Decision 031 (M3.1 acceptance and
evidence bindings); Decision 023 §7 O1 (stop-and-refer); `Docs/m3/operator_runbook.md` steps 16–28
and Appendix B (the planned `m3 acquire`, `m3 acquire --show-scope`, and
`m3 derive-dependent-plan` interface contracts); `Docs/m3/execution_receipt_spec.md`
(`m3-execution-receipt/2.0`); `Docs/m3/templates/gate_h_checklist.md`, `request_budget.md`,
`interrupted_run_recovery.md`, `schema_drift_incident.md`, `evidence_index.md`;
`Docs/leakage_register.md` (L01, L04, L05, L10, L18); CLAUDE.md rules 4, 6, 9, 12.

## 5. Accepted inputs and immutable identities

The following are **frozen inputs** to M3.2A. None may be re-derived, re-planned, edited, or
substituted; a mismatch at any gate is a stop condition:

| Input | Identity |
|---|---|
| M3.2A request plan (two byte-identical zero-request plans) | SHA-256 `19be7bdc9071d0dcdcaaa1972e6b4844fa8076c9b1761735f903fa500623af68`; schema `m3-request-plan/1.0`; planner `quarterly-index-instances/2.0` |
| Owner-approved request budget (M3.2A window) | SHA-256 `2d453e0b6d1b65b0d474d454e4fa1540fb615b1c78572956acdb2cfcb17cab3f` |
| **Hard request ceiling (M3.2A)** | **801** — owner-approved 2026-08-03, bound to the plan hash above |
| Planned unique logical requests | 75 |
| Required quarterly-index instances | 70 (`2009QTR1`–`2026QTR2`, closed 2026 Q2 included per Decision 013 §1) |
| Maximum new raw objects | 75 |
| Expected cache hits | 0 (catalog nonexistent at planning; excluded before planning, never subtracted again) |
| Contingency | **none — prohibited** (Decision 027 §0; master plan §§15–16) |
| Plan inputs | coverage 2009-01-01 → 2026-06-30; as-of 2026-06-30; calendar year 2026; explicitly empty operator calendar-evidence manifest (`edgar-calendar-evidence/1.0`); requests_per_second 4.0 |
| Rate-limiter spacing floor | 200.0 s = max(0, 801 − 1) ÷ 4.0 — a floor, **never** a claimed maximum duration |
| Per-route `A_reachable` (derived and independently witnessed at M3.1A) | singletons 6; `sec_edgar_filing_calendar` 7; `sec_edgar_calendar_announcement` 6; `sec_full_index_company` 11 |
| Three response-outcome expectations | `EXACT_COUNT_RESOLVED_BY_GATE_F_ZERO_REQUEST_PLAN` — deliberately unresolved (Decision 030 Ruling C); resolved by the actual controlled acquisition, never guessed |

**A stale budget is not an approved budget** (master plan M3.2 §5.2): the exact M3.2A integers
must be re-confirmed by the owner at the §8 live-operation authorization, restated against the
same plan hash.

## 6. M3.2A route and source boundary

**M3.2A permitted routes — the seven bootstrap families only**, from the accepted source registry
`m2.2-source-registry/1.0`, hosts `www.sec.gov` and `data.sec.gov`, method `GET` only, expected
content types per master plan M3.2 §12:

`sec_bulk_submissions` (1×6), `sec_company_tickers_exchange` (1×6), `sec_company_tickers` (1×6),
`sec_sic_code_list` (1×6), `sec_edgar_filing_calendar` (1×7),
`sec_edgar_calendar_announcement` (0×6 — manifest-resolved only, never arbitrary-URL; `U = 0`
lawfully from the explicitly empty operator manifest), `sec_full_index_company` (70×11).

**Permitted metadata fields:** exactly the master plan M3.2 §12 list (canonical CIK; registrant
and former names with ranges; tickers/exchanges as noncanonical aliases; SIC; fiscal year end;
accession number; form type; official filing date; acceptance timestamp; report date; XBRL flags;
quarterly-index registrant-per-accession rows; SIC reference codes; operating-calendar evidence).

**M3.2B routes** (`sec_submissions_historical`, `sec_submissions_entity`) are **not in the M3.2A
window** — see §15 (boundary) and §17 stop condition 2.

## 7. SEC identity and secret handling

The SEC contact identity is validated **at the canonical boundary only** (`SEC contact identity:
valid; value not displayed`); the value lives only in the ignored local configuration, is resolved
on demand, and is **never** printed, echoed, logged, hashed into a governed identity, serialized
into a plan, receipt, report, or evidence artifact, or included in any backup. `.env` is ignored
and is excluded from every snapshot, scan, and evidence set. No credential, token, cookie, or
authorization header is ever recorded anywhere, public or private (master plan §12.4 item 6;
receipt prohibited-field rules).

## 8. Network enablement controls — the owner gate ladder

Network and CompanyFacts remain **disabled** in the effective configuration at all times outside
an authorized window. **Each consequential transition below requires its own separate, explicit
owner instrument; none may be inferred, combined, or inherited** — and in particular the Gate F
readiness token, the ceiling approval, and this contract's future acceptance are each **not**
live-operation authorization. The substantive six-transition separation (the repository's
canonical gate names are used where they exist; no duplicate "Gate A–F" names are created —
canonical **Gate F** is the completed M3.1 readiness gate and canonical **Gate H** is this phase's
post-acquisition integration gate):

| # | Transition | Owner instrument required |
|---|---|---|
| T1 | **Contract acceptance** — this draft becomes accepted, after its independent review | Owner acceptance recorded in this header, the registry where applicable, and the ledger |
| T2 | **Implementation authorization** — bounded code/test work on the §16 paths may begin | Separate explicit authorization under all five Decision 024 §8 conditions |
| T3 | **Implementation acceptance** — the frozen M3.2 implementation is accepted | Independent review by a non-author session (§19), then owner acceptance |
| T4 | **Live-operation preflight** — runbook steps 16–18 pre-run state proven | Operator preflight evidence complete; no instrument by itself enables anything |
| T5 | **Live-operation authorization (M3.2A)** — one bounded acquisition operation | A separate, explicit owner instrument naming the exact command invocation, window `M3.2A`, plan hash `19be7bdc…`, ceiling `801`, and the configuration change enabling network for `m3 acquire` only |
| T6 | **Controlled acquisition execution (M3.2A window)** | Only after T5; network disabled again at window end, before the freeze/derivation step |

**M3.2B repeats T4–T6 with its own plan, budget, ceiling, and owner approval** after the
between-windows freeze (§15). **Gate H** then integrates both windows (§14).

**T4 preflight must verify, at minimum:** the repository checkpoint (`m3.1-complete` and the
accepted M3.2 implementation commit); a clean synchronized branch (`HEAD == origin/main`); the
accepted implementation SHA; SEC identity valid at the boundary without disclosure; network and
CompanyFacts disabled **before** authorization; the request-plan and budget identities (§5) and
the ceiling 801; the external evidence root valid (`require_external_evidence_root`); backups
current, with the owner's off-device-backup decision recorded (§20); the operator runbook
acknowledgement; the exact operation command string; the stop rules of §17; and the Gate H pre-run
state (isolated M3.2 data root; consistent SQLite backup of accepted prior state; recorded
available storage; confirmed quarantine and staging paths; the single-writer lock; no stale
`.part` files; no unresolved recovery events; the approved plan hash saved).

## 9. Request-plan and ceiling controls

- The run **consumes** the approved plan; it never re-derives or recomputes one mid-window
  (master plan M3.2 §15).
- `m3 acquire` requires `--plan` (matching hash `19be7bdc…`), `--window M3.2A`, an explicit
  `--live` flag with no default, and `--ceiling 801` exactly equal to the approved integer; a
  mismatch on any of these refuses before any transport construction.
- The shared cumulative physical-attempt ceiling gate (`sec.request_ceiling`) is supplied
  explicitly — never defaulted or inferred — and **refuses the attempt that would exceed 801**:
  stop-before-overflow, never stop-after. A complete run may finish exactly at 801; equality with
  planned work remaining is `stopped_at_ceiling`, records `SEC_REQUEST_CEILING_EXHAUSTED`, and is
  a Gate H failure. The ceiling is **never raised mid-window**; more headroom requires stopping,
  re-planning, and a new owner approval.
- Every retry, redirect hop, and controlled post-cooldown request consumes one physical attempt;
  the accepted response policy applies unchanged (5 transient retries, 60 s backoff ceiling,
  `Retry-After` honoured when usable, one 600 s aggregate cooldown with one controlled further
  request, second cooldown terminal; retryable statuses 408/500/502/503/504; failure never becomes
  an empty success).
- Actual counts are recorded per route and reconciled item-by-item against the plan at Gate H; a
  deviation is a finding, not a variance.

## 10. Raw-object and receipt requirements

- Every successful retrieval stores an **immutable, content-addressed raw object** with complete
  Decision 009/master-plan §21 provenance (content, transport, and stored-object hashes; relative
  path; compression; attempt identity; `retrieved_at` UTC; validator metadata; the complete
  validated redirect chain; parser identifier/version/status; schema fingerprint; supersession
  lineage), carrying accession, CIK, form type, filing date, acceptance timestamp, fiscal period
  end, and source offsets through every derived row.
- A byte-identical body reconciles to the existing object; **a differing body is a new
  observation, never an overwrite**; raw data are append-only and never deleted (CLAUDE.md rule 6).
- **One `m3-execution-receipt/2.0` receipt per live command**, validated, immutable,
  content-named, recording the master plan §22 field set (plan identity; ceiling; planned and
  actual counts; classification totals; raw/duplicate/cache counts; drift outcome; completion
  status; reason code; interruption state; predecessor receipt on resume) and containing **none**
  of the prohibited fields. Receipts and operational values contaminate no governed identity.

## 11. Deterministic storage, provenance, and identity effects

Storage: the isolated M3.2 data root and the operational catalog
(`catalogs/m3_2a_operational.sqlite3` relative to the external evidence root, created only inside
an authorized window at migration chain `0013`), raw store, and JSONL projections — all below the
owner-controlled external evidence root, never inside the repository. **Migration effects: none**
— no `0014` exists or is implied. **Identity effects:** M3.2 produces operational identities only
(receipt_ids, content hashes, observation rows); no candidate, selection, reserve, manifest, or
release identity is produced, and the suite-level non-contamination proof (S5/S6 identities
byte-identical with receipts absent/present/varied) must keep passing unchanged.

## 12. Interruption, restart, and recovery

Per master plan M3.2 §§27–29 and `interrupted_run_recovery.md`: on interruption, stop new
requests; preserve every attempt and committed object; quarantine partial/unverifiable objects;
roll back the uncommitted transaction; rebuild the projection; run integrity and FK checks; write
the terminating receipt; **require an explicit owner resume-or-new-run decision**. Resume only
after the M3.1 read-only inspector (`m3 recovery-state`) reports `SAFE` and duplicate prevention
is proven; the consumed physical-attempt count carries forward under the same ceiling; a resumed
receipt names its predecessor. **Recovery uncertainty is a stop condition** — if it cannot be
determined whether a write committed, the run does not resume. Nothing acquired is ever deleted.

## 13. Leakage and filing-body prohibitions

Binding at runtime, not only in assertion (master plan M3.2 §§13, 20): **zero filing-body
access** — no `/Archives/edgar/data/` accession-archive path, no `-index.htm`, no `.txt`/`.htm`/
`.xml`/`.xsd` filing-document suffixes, no primary documents, complete submissions, SGML headers,
exhibits, Inline XBRL, XBRL instances, or taxonomies; **zero CompanyFacts and zero Frames**; no
financial-outcome source; no non-SEC host; no method other than GET; no external corpus (L18); no
outcome value; **no pilot selection, snapshot, reserve, sealing, manifest, or root work during
acquisition** (deferred to M3.3+); L01/L04/L05/L10 and the Decision 010 filing-date rule apply as
written. Leakage rules 4 and 5 of CLAUDE.md remain absolute.

## 14. M3.2A completion criteria and Gate H

M3.2A completes only when: every planned logical request is satisfied, reused, or terminally
classified with a registered reason; actual counts reconcile to the plan (`actual ≤ ceiling`,
zero overflow); zero prohibited-route attempts; zero unresolved blocking drift; every stored
object fully provenanced and hash-verified; network disabled again; every receipt validated; and
the between-windows freeze recorded the exact bootstrap object identities. **Gate H** (canonical
name; `gate_h_checklist.md`) runs only after M3.2B and integrates **both** windows: per-window
planned-versus-actual reconciliation, raw-store and provenance completeness, classification
totals, drift outcomes, recovery records, receipts, and the network-disabled confirmations. Phase
token, emitted only by the canonical command path once every Gate H item passes:
`M3_2_METADATA_ACQUISITION_COMPLETE_GATE_H_PASSED`.

## 15. M3.2B dependency boundary (governance only in this contract)

- M3.2B **cannot be planned, budgeted, approved, or begun** until M3.2A completes, transport is
  disabled, and the bootstrap raw objects are **frozen and identified**.
- Its request plan is produced by the future `m3 derive-dependent-plan` command **offline over the
  frozen M3.2A objects** (historical-file references from the frozen bulk-submissions object; the
  explicit entity reconciliation set) — **zero requests** — and its budget instantiates the
  request-budget template for the M3.2B window.
- It requires a **separate exact owner ceiling approval**; **it may not inherit, reuse, or extend
  the M3.2A ceiling 801 as any form of blanket authorization**, and no M3.2B count is stated or
  invented in this contract (both are `EXACT_COUNT_RESOLVED_BY_GATE_F_ZERO_REQUEST_PLAN` until
  derived).
- The derived reference set must match what the frozen objects actually name; a disagreement is a
  stop condition.

## 16. Authorized and prohibited paths (for the later, separately authorized implementation)

**Nothing below is authorized by this draft.** Upon T2, the implementation authorization must
enumerate the exact final path set; this draft records the expected surfaces, all within master
plan M3.2 §9 categories:

- **Implementation (expected):** `src/disclosure_drift/cli.py` (the `m3 acquire`,
  `m3 acquire --show-scope`, and `m3 derive-dependent-plan` wiring with the explicit `--live`
  flag); a new bounded acquisition-driver module under `src/disclosure_drift/m3/` and, where the
  contract of record at T2 names them, bounded edits to the existing census-orchestration and
  index-retrieval surfaces (`sec/census_orchestrator.py`, `sec/index_retrieval.py`); receipt
  emission call sites for live commands; the single, explicitly named network-enable
  configuration change.
- **Tests (expected):** new unit/integration tests for the §18-listed families, plus bounded
  edits to the existing M3 CLI and transport test modules.
- **Private evidence paths (never tracked):** the external evidence root's M3.2 run directories,
  raw store, catalogs, receipts, plans, budgets, checklists, and recovery records.
- **Operational-catalog path:** `catalogs/m3_2a_operational.sqlite3` under the evidence root —
  created only inside an authorized window.
- **Prohibited (unchanged from master plan M3.2 §10):** every accepted S4/S5/S6 module; every
  migration; `cohorts.py`; `pilot_policy.py`; `paths.py`; `config.py` beyond the one named
  network-enable change; `release/`; `sec/pilot_manifest_store.py`; `configs/` beyond that same
  named change; `.github/`; `Docs/preregistration.md`; Decisions 001–031; every completed
  contract; `tests/integration/test_no_network.py` (byte-identical and passing); and any path that
  could retrieve a prohibited route. **No tracked path may ever contain raw data, a database, a
  receipt, private evidence, or any part of the SEC identity** (`make hygiene` enforces).

## 17. Mandatory stop conditions

Immediate fail-closed stop, report, and owner referral on any of (master plan M3.2 §17 plus this
contract): (1) identity validation failure, or the identity appearing in any output, log, or
artifact; (2) network or CompanyFacts unexpectedly enabled outside an authorized window, or
enabled before the T5 instrument exists; (3) plan or budget hash mismatch; (4) ceiling mismatch,
any attempt beyond 801, or reaching the ceiling with planned work remaining; (5) any unexpected
route, host, method, redirect (loop, over-depth, out-of-family, identity-path change), or content
type — including a dependent request in M3.2A or a bootstrap request in M3.2B; (6) any response
the accepted policy cannot classify; (7) a second aggregate cooldown, or 403/block-page after the
one controlled retry; (8) attempt-counter inconsistency; (9) receipt inconsistency or a
prohibited receipt field; (10) an existing-object hash conflict, a raw object failing
`content_sha256` or lineage verification, or any unexpected mutable overwrite; (11) a changed
body at an immutable or closed-quarter dated identity without official explanation; (12)
unresolved blocking schema drift, or a new historical-file reference that would expand the plan;
(13) external-evidence-root validation or permission failure, or symlink detection at any
governed path; (14) insufficient disk space, or any `.part` file / orphan / recovery event that
cannot be resolved deterministically; (15) **D023-O1 triggered** — an empty sole-carrier
crosswalk family in any lawful real run: stop and refer to the owner, never reclassify (Decision
030 Ruling E); (16) any filing-body or prohibited-content exposure; (17) clock or as-of mismatch
where a governed input names one; (18) Git baseline mismatch at any gate; (19) an unresolved
BLOCKER or relevant MAJOR finding at any review boundary; (20) catalog quick/integrity/FK check
failure; (21) M3.2B beginning without its own derived plan and recorded owner approval, or the
derived reference set disagreeing with the frozen objects. **No workaround, widening, default,
coercion, skip, or silent relaxation is permitted** (CLAUDE.md rule 12).

## 18. Required tests (minimum categories, at T2/T3)

Per-window budget enforcement and the stop-before-ceiling boundary (C−1, C, C+1; carried-forward
counts; shared gate across logical retrievals); ceiling derivation from the implemented state
machine; per-route allowlist/denylist enforcement including window-scoped route separation;
`--live` explicitness, plan-hash binding, and ceiling-argument equality refusals;
`m3 acquire --show-scope` zero-request scope printing; resumability with no duplicate substantive
write; duplicate-object reconciliation and 304 handling; blocking-drift refusal and
retained-unknown-field logging; dependent-plan derivation from a frozen object fixture (offline);
receipt completeness, mode classification, prohibited-field positive control, and identity
non-contamination; recovery SAFE/UNSAFE/UNDETERMINED integration; and
`tests/integration/test_no_network.py` byte-identical and passing for every non-authorized path.
The `[sec]` extra installed with `tests/unit/test_httpx_transport.py` running, not skipped. No
test may depend on Git history, the clock, the real identity, a machine path, network access, or
a real SEC response.

## 19. Validation and independent-review requirements

- **During implementation (post-T2):** targeted tests via `make fast` and
  `make test PYTEST_ARGS="<paths>"` chosen from `Docs/change_impact_map.md`.
- **At T3 (implementation acceptance):** the fixed full sequence — `ruff check .`;
  `ruff format --check .`; `mypy src`; `pytest` (full suite; transport test runs);
  `make sqlite-check`; `make secrets`; `make hygiene`; `make context` — plus
  `tests/unit/test_migration_provenance.py`, receipt validation, request-plan validation, and the
  ceiling/stop-before-overflow validations; then a **focused independent review by a fresh
  session that authored none of the M3.2 work**, with a durable artifact under
  `Docs/m3/reviews/`, before any live operation.
- **Post-operation (per window and at Gate H):** planned-versus-actual reconciliation; raw-store
  and provenance completeness; response-policy compliance with zero unclassified responses; zero
  prohibited-route attempts; zero overflow; zero unresolved drift; zero identity leakage; catalog
  quick/integrity/FK checks; proof the M3.2B plan derived from the frozen objects; proof
  transport was disabled between windows; receipt and artifact validation; then the **independent
  M3.2 acceptance review** (master plan M3.2 §26) by a session that ran none of the acquisition.

## 20. Evidence and backup requirements

All completed operational evidence lives below the **owner-controlled external evidence root**
(mode `700`, artifacts `600`, regular files only, symlink-resistant validation), create-once and
immutable, with SHA-256 identities; one receipt per live command; the public
`Docs/m3/templates/evidence_index.md` receives **only** artifact type, phase, status, SHA-256,
and a non-sensitive reference identifier — never a path, identity, or content. **Same-device
snapshot backups are required at every window boundary and before/after each governance
recording, and are explicitly accidental-deletion protection only, not disaster recovery; the
owner must record an explicit off-device-backup decision at T4, before any live operation.**
`.env` is excluded from every backup and every scan. This draft creates no evidence and no backup.

## 21. Operator workflow

The operator follows `Docs/m3/operator_runbook.md` steps 16–28 exactly as written once the
commands exist: enable (T5) → verify scope (`m3 acquire --show-scope`, zero requests, compare
against Gate F evidence, stop on any difference) → establish Gate H pre-run state → execute the
one authorized `m3 acquire` invocation → verify totals, receipts, provenance, and clean
repository → disable network → freeze and derive (offline) → second approval → repeat for M3.2B
→ Gate H. The identity is never printed; a planned command is never typed before it exists; the
stop rule applies at the first mismatch (runbook Appendix C).

## 22. Commit, checkpoint, and completion-report requirements

- **One implementation commit by default** (post-T2), containing only the T2-enumerated paths; an
  intermediate checkpoint only if this contract is amended to justify one and the owner
  separately authorizes it. No commit ever contains data, a database, a raw object, or a `.part`
  file.
- **Governance-only acceptance commit** for M3.2 acceptance (master plan M3.2 §33), changing no
  implementation byte.
- **Annotated tag `m3.2-complete`** only after the independent M3.2 acceptance review, at the
  accepted commit, under explicit owner tag authorization (master plan M3.2 §34). No other tag.
- **Completion-report fields (every M3.2 implementation or operation session):** BASELINE_HEAD;
  BASELINE_TAGS; CONTRACT; AUTHORIZED_PATHS_TOUCHED; PROHIBITED_PATHS_TOUCHED (`none`); WINDOW;
  PLAN_SHA256; APPROVED_CEILING; ACTUAL_LOGICAL_REQUEST_COUNT; ACTUAL_PHYSICAL_ATTEMPT_COUNT;
  RESPONSE_CLASSIFICATION_TOTALS; NEW_RAW_OBJECTS; DUPLICATES_RECONCILED; CACHE_HITS;
  DRIFT_OUTCOME; RECEIPTS_EMITTED (ids); RECOVERY_STATE; TARGETED_TESTS; FULL_VALIDATION;
  NONCHANGE_PROOF; LIMITATIONS; COMMIT_STATUS; NEXT_AUTHORIZED_ACTION — plus the CLAUDE.md
  narrative completion packet.

## 23. Limitations and residual risks

Inherited and binding: all 35 open register entries, including D020-L1–L5, Decision 021 §19,
Decision 022's boundary, Decision 023 §7 O1–O4 (O1 = §17 stop condition 15), Decision 024 and
026 standing obligations, and the ten open Milestone-3 entries. Residual risks this phase must
manage: same-device-only backups until the owner's T4 off-device decision; the three deliberately
unresolved response-outcome expectations (resolved only by the actual run); unbounded elapsed-time
factors above the 200.0 s spacing floor (acknowledged, never estimated); and SEC-side variability
(drift, blocks, calendar events), all of which fail closed under §17.

## 24. Negative authorizations (what this contract, even once accepted, does not do)

Acceptance of this contract does **not**: authorize implementation (T2 is separate); enable
network or CompanyFacts; authorize any HTTP request to the SEC or any live operation (T5 is
separate and per window); authorize the operational catalog outside an authorized window;
authorize any M3.2B planning before the freeze; convert the readiness token, the ceiling
approval, or any M3.1 artifact into operation authority; authorize any M3.3+ work (snapshot,
selection, reserve, manifest, root, publication, outcome access); create or move any tag; or
alter any frozen research definition, accepted identity, migration, or prior decision. Every
Decision 024 §8 condition applies in full at every later transition.

## 25. Stop-and-report boundary for this draft

This document ends the Decision 029 §12 sequence (step 17). Further M3.2 progress requires, in
order: the owner's review of this draft; its independent contract review; owner acceptance (T1);
and the separate T2 implementation authorization. If review finds any conflict with an accepted
record, the draft is corrected — accepted records are never adjusted to fit it.
